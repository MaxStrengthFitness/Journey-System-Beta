# The Client Codex — Notes & Profile as seven pages

*Sep 24 2026, overnight. Branch `client-codex`, one commit per phase. **Nothing is pushed or deployed** until AJ says go: the phases accumulate on the branch (AJ's decision). The plan is `INTEGRATION.md` and the six area specs in the session scratchpad; the approved mockup is `client-codex.html`. This document is started in phase 2 and finished in the last phase (the full story, the baselines, how it ships, and the index and CHANGELOG rows).*

## Phases so far

- **1 — FORD reads that work for every trainer, and honest test commands** (`29f653d`). `useClientFord` listed FORD with no studio filter, so the rules refused it for everyone below franchise owner and the refusal read as "nothing on file". Now filtered on the client's studio, with a `status` that tells "couldn't be read" and "kept by the home studio" from empty. `useClientJournal` reports what it could read and hands out the sessions it already streams. The test commands skip the `.claude` worktree copies. See KNOWN-TRAPS → The client profile.
- **2 — The InBody normal variation (AJ's decision 8)** (`9e77640`). Below.
- **3 — Navigation: the record tab becomes pages** (`b02e66d`). The record arm of the profile's one location is `{ page, anchor }`; every old section and tab id still lands; the tab always opens on the Overview. See `src/features/client-profile/README.md`.
- **4 — The sub-toggle can wrap** (`5884295`). `ProfileSubnav` gains `wrap`, `idPrefix` and a plum `warn` dot, all opt-in, so Programming and the Activity Archive are unchanged. See KNOWN-TRAPS → The client profile.
- **5 — One visual kit** (`c8203b9`). Below.
- **6 — Additive props and exports the pages need** (`b751b9f`). Below.
- **7 — Thread selectors and the critical line** (`f52a748`). Below.
- **8 — The shell: seven pages, one Save bar, one load** (`cfa13f7`). Below.
- **9 — The Notes page** (`1a25a71`). Below.
- **10 — The FORD page (without the new fields).** Below.

## Phase 2 — the scanner's normal variation

An InBody scan of the same body reads a little differently every time, and until now every screen called any difference a change: "+1.2 lb muscle" was proof on the renewals pipeline, made a client an upgrade candidate, and printed green on the progress report the client takes home.

**The rule** (`src/features/inbody/variation.ts`): a change smaller than the client's HOME studio's number is "within the scanner's normal variation" and no screen calls it up, down, better or worse; a change equal to the number or bigger is called. The number itself is always shown. Max Strength's defaults are **3.5 lb** skeletal muscle, **5.3 lb** body fat mass and **2.7 points** body fat %. Fat mass and body fat are the cautious end of McLester et al. 2020 (checked against the abstract tonight: 2.12–2.73 %, 1.49–2.39 kg fat mass, 1.60–2.32 kg fat-free mass). The study did not measure skeletal muscle: 3.5 lb is AJ's figure, the low end of the fat-free-mass range. Weight has none and is never toned.

**Each studio sets its own** on My Studio → Studio → *InBody: the scanner's normal variation*, between the studio's day and Renewals. It is stored at `studios/{id}.inbodyVariation`, written whole with `updatedBy` = the Auth uid; "Use Max Strength's defaults" removes the field. **No rules change**: the studio update rule already scopes the write to the studio's own leaders (and the grant), franchise owners and administrators — pinned by four new rules tests ("Studio InBody variation"). No index change. No read: the studios are already in memory.

**What is stored never changes.** `clients/{id}.inbodySummary` and the nightly `renewal.proof.inbody` keep the raw changes, and the nightly job is untouched. Lowering a studio's numbers brings a hidden change straight back.

### The wording, before and after

At the defaults, for the fixtures the tests pin (a client up 2.3 lb of muscle and down 1.8–2.2 points of body fat):

| Where | Before | After |
| --- | --- | --- |
| **Progress report** — Body Composition, Change column | `+2.3 lb` (green), `−2.2 pts` (green) | `+2.3 lb · within normal variation`, `−2.2 pts · within normal variation` (uncoloured), and one line under the table: *"Within normal variation: smaller than the difference an InBody scanner can show between two scans of the same body, so it isn't counted as a change."* A change beyond the variation prints as before, e.g. `+4.1 lb` (green) |
| **Renewal Brief** — health, the InBody line | `InBody since Jan 15: muscle up 2.3 lb, body fat down 1.8 points` | `InBody since Jan 15: no change bigger than the scanner's normal variation`. Only a called change is named: `InBody since Jan 15: muscle up 4 lb, body fat down 3.1 points`, or just `… muscle up 2.3 lb` when a studio's muscle number is 2 |
| **Renewal Brief** — upgrade reasons | `InBody: muscle up 1.2 lb.` counted as visible progress | only muscle up, or body fat down, beyond the variation counts; otherwise "Progress isn't visible in the data yet." |
| **Pipeline row and the profile's renewal card** — the proof line | `In 11 of the last 12 weeks · stronger on 12 of 14 machines · +2.3 lb muscle` | `In 11 of the last 12 weeks · stronger on 12 of 14 machines` (the muscle part returns at `+4 lb muscle` and above) |
| **Pipeline** — "Upgrade candidates" filter | a client whose only progress was +1.2 lb of muscle was a candidate | not a candidate at the defaults; each row is judged by its client's home studio's numbers |
| **InBody card** — the sentence | `Since Jan 15: muscle up 2.3 lb, body fat down 2.2 points.` | `Since Jan 15: no change bigger than the scanner's normal variation.` — or `Since Jan 15: muscle up 4.0 lb; body fat within the scanner's normal variation.` when one is called |
| **InBody card** — the tiles | `+2.3 lb` (green) | `+2.3 lb · within normal variation` (uncoloured) |

### Files

`src/features/inbody/variation.ts` (+ test), `useInBodyVariation.ts`, `scans.ts` (`formatCalledChange`; `changeTone` and `summarySentence` take the variation), `InBodyCard.tsx` (+ render test), `InBodyReportSection.tsx`; `src/components/ClientProgressReportView.tsx`; `src/features/renewals/brief.ts`, `sentences.ts`, `options.ts`, `pipeline.ts`, `RenewalCardDialog.tsx` (+ the three tests); `src/features/admin/renewals/RenewalsPipeline.tsx`, `RenewalBrief.tsx`; `src/features/my-studio/InBodyVariationPanel.tsx` (+ render test), `StudioSection.tsx`; `src/types.ts` (`Studio.inbodyVariation`); `tests/firestore.rules.test.ts`; the InBody and My Studio READMEs, KNOWN-TRAPS and ARCHITECTURE.

## Phase 5 — one visual kit

Every codex page is built from one kit, so a trainer learns one panel, one button and one set of words: `src/features/client-codex/kit/` (read `src/features/client-codex/README.md`). Nothing on screen changes in this phase — the kit has no caller yet; the shell (phase 8) and the pages use it.

- **Tokens** (`codex.tokens.css`): every colour is an alias of a token the app already has (`--eq-*`, `--ford-*`), so there is no new palette and light and dark come for free. Text is 11 / 12 / 14 / 17 / 30px and nothing else.
- **Pieces** (`primitives.tsx`, `ReadEdit.tsx`, `fields.tsx`, `SaveBar.tsx`, `kit.css`): Page (head, ‹ › neighbours, Next card), Card, Slot, FactList, Rows, Chip, LoudChip, Btn, FordMark, the read-view-then-Edit frame, the inputs and pick pills, and the one Save bar ("1 unsaved change · FORD · Occupation"). Nothing tappable is under 40px; nothing is clipped; solid buttons are brand blue, never orange.
- **Words** (`text.ts`, `src/lib/first-sentences.ts`, `pronouns.ts`): whole sentences instead of "…" (a critical note is never cut mid-instruction), dates the studio way, and she / he / they from the gender Mindbody holds — they when it holds none (the overnight default for AJ's pronoun question; one file to change). The review caught `firstSentences` ending "Train her at 7 a.m. only…" after "a.m." and "Ask "does it pinch?" before every set" after the question mark; the word after the mark now decides (a lower-case word or a number means the sentence goes on), so an abbreviation it does not know makes the line longer instead of cutting it. `inTime` counts days up to 30 ("Birthday in 17 days", as the mockup says), and FORD will use it rather than a second formatter.
- **Loudness**: `LOUDNESS_TONE` is exported from `src/features/rating/Loudness.tsx`, so the codex shows Heads up plum and Critical crimson with the one control's own mapping (a departure from the mockup's gold, as agreed).
- **The contract is a test** (`kit/scale.test.ts`): it reads every codex file and fails on an off-scale text size, a raw hex colour or a colour a component sets itself, clipped text (a line clamp is allowed only on the two named row-body selectors, at two lines), a regex lookbehind or a raw invisible character, and on any file in the folder that is not on its list. Adding a 13px rule to `kit.css` fails it (checked). `kit/kit.render.test.tsx` mounts every piece in light and dark and checks each control is at least 40px tall.
- **Accessibility**: a field's label is its name and its hint its description, so VoiceOver reads each once; the Save bar announces the first unsaved change through a live region that is mounted before it.

No Firestore, rules, index or Functions change.

## Phase 6 — additive props and exports the pages need

The codex loads each thing ONCE for the whole tab and hands it to every page. This phase lets the existing pieces take what the tab loaded instead of reading it again, and exports the small readers the pages need. **No screen changes**: every new prop is optional, and a caller that passes none of them behaves exactly as before (the existing notes, Pulse panel, goals and contract tests pass unchanged).

- **Shared data, one listener each.** `FordSection` takes `ford` (the tab's FORD stream), `InBodyCard` takes `inbody` (the tab's scans), and `ClientCheckInPanel` takes `draft` (the Body & Pulse page's one Pulse draft). Given one, each disables its own hook — it is not opened a second time. For the Pulse this is more than cost: two drafts of one client autosaving side by side is the duplicate-draft bug. The panel also takes `startInClientMode` (opens client mode once the draft is in, and again each time it turns true, so "Hand to client" on the page is one tap) and `onClientModeClose`; client mode never opens over a draft still loading. `CheckInDraftState` is exported.
- **Readers exported, not rewritten.** `statementAnswer` (a saved round's own answer to one statement on 0–10 — a v1 answer converted, `null` when the round did not ask; it was the private `ownAnswer`) and `painKeyOf` (`region:side`, how a spot is paired with last round's). `ContractTermRow.cancelledAt` — when Mindbody said a contract was cancelled; null unless the row is cancelled (a re-activated contract with a stale stamp never reads as cancelled) and for paid-in-full rows. Story will read it.
- **`useFocusActions`** (`src/features/goals/`): set, achieve, extend, retire and check in on a focus, moved out of `ClientJournalTab` unchanged (author = the Auth uid, the check-in carries its focus id). The journal's Focus area uses it now; Goals & Focus will.
- **`useNoteDismissalsState`**: the trainer's dismissals plus whether they have been read (`loading · ready · failed`), keyed by uid so another trainer never inherits the map. The Notes page offers hush / restore only once it is `ready`. `useNoteDismissals` (the briefing's) returns the same map as before.
- **ClientProfileView** gains `progressReportsStatus` (whether the progress-reports listener answered for THIS client — Pulse history is read from that list, and a list never read must not pass for "no Pulse on file") and `journeyCompletedCount` (the Journey session count, null until the count answers — the header's number starts at 0). The profile is not remounted per client, so both are stamped with the client they are for and read through `answerFor` (`src/features/client-profile/client-answer.ts`). Nothing reads them yet; the shell (phase 8) passes them to the codex. Out of quota, the reports listener never opens, so the status says `failed` rather than waiting forever.

Tests: the new props each have a render case (the panel with the page's draft makes no read of its own — checked by count against the panel alone — and writes once; client mode waits for the draft and re-opens on a second tap; FORD and InBody open no listener when handed their data and say "couldn't be read" / "loading" rather than "nothing"); `useFocusActions` mounts on its own and through the journal's Focus card; the dismissals status, `answerFor`, `cancelledAt`, `statementAnswer` (v1 and v2) and `painKeyOf` have unit cases. No Firestore, rules, index or Functions change.

**Carried to phase 8.** Nothing mounts `ClientProfileView`, so the wiring that stamps `progressReportsStatus` (the listener's success and error callbacks) has no test: dropping the `failed` stamp would leave Pulse history "loading" forever with the suite still green. Phase 8 moves the reports listener into a small hook with its own render test (ready, failed, a client switch, out of quota). The codex also stays keyed on `client.id`: `draft`, `ford` and `inbody` are not checked against the panel's `client`, so a stale hand-over is prevented only by that key.

## Phase 7 — thread selectors and the critical line

Five of the seven pages talk about notes, and each area's spec had written its own version of the same selection — one "how to coach her" pulled in every legacy session wrap-up and labelled it a Preference; one "her notes on this machine" dropped every standing set-up note. Each selection now lives ONCE, in `src/features/client-notes/record-selectors.ts`, and every page will read the tab's one journal load through it. **No screen changes yet except one fix** (below): the shell (phase 8) and the pages use these.

- **What Notes lists** — `notesOnRecord`: the five profile fields the record edits elsewhere stay off, an unfiled note goes to the To-file tray, and a life note that is no longer live is marked *settled* (FORD's to show, "from an older note"). Settled life notes **stay on Notes until the FORD page carries them** (`lifeOnFord`, default false; the FORD phase turns it on), so no note ever leaves the tab in between. A note is settled only if `olderLifeNotesByPillar` will place it — one test, `isOlderLifeNote`, for both — so a `client.events` copy and an update whose root is out of the load (an orphan, which stands as a thread of its own) stay on Notes rather than falling between the two pages; a test asserts the two selections are the same set.
- **Counts** — `notesSummary` (takes `notesOnRecord`'s result whole, so the To-file tray is counted; critical is the briefing's own count), `notesTabMeta` for the sub-toggle ("3 open · 1 critical"; "2 to file" when the only notes wait for a category; nothing while loading; "couldn't load", never "none yet", when the read failed), `notesSummarySentence` for the Overview ("… · 2 to file"; "No notes in Journey yet" only when the tray is empty too).
- **One thread in words** — `whoOf` ("Jess"; the source for an import), `shortDay`, `threadRowMeta` ("Jess · Nov 3 · closed Nov 7", "· ended Oct 3" when the window ran out), `closeWordsOf` ("All healed up" / "It's back" for an injury or incident only — an equipment note simply closes), `briefingStatusOf` (on the briefing, hushed by you, from a date, or gone quiet after three weeks; "on" is marked unchecked until the trainer's dismissals are read).
- **The other pages** — `howToCoachThreads` (Preference and Coaching-tip notes by KIND, so legacy wrap-ups stay out; focus check-ins stay on their focus), `injuryThreads` (by the catalog's category, optionally with incidents; the medical fields are left to Body), `threadsByMachine` (every open or standing note on a machine, loudest first — no focus check-ins, which would bury the set-up notes), `olderLifeNotesByPillar` (Birthday and Anniversary → Family, Vacation → Recreation; the `client.events` rows FORD already draws are left out), `fordDoorCount` (the "Life · in FORD" door: FORD's own count once read; while it loads, the client document's counts — never its text — plus the `client.events` rows FORD adds, so the number does not jump when FORD arrives; **null, not 0,** when unknown or when the reader can't open FORD), `hiddenCriticalThreads`.
- **The critical line** (`CriticalLine.tsx`, `critical-line.css`): "Critical · Leg Press: stop at 90° at the bottom turn." — the briefing's selection, ignoring dismissals (the record never hides a note), the machine named in full, and **whole sentences, never cut and never "…"** (`firstSentences(body, 90)`; the line wraps). "· and 2 more critical notes" when there are more. With `failed` it says *"Notes couldn't be loaded, so a critical note may be missing."* instead of drawing nothing; that state is neutral, not crimson, because it is not a critical note. It imports nothing from the codex; its three files are on the scale test's list (checked: a 13px rule in `critical-line.css` fails it).
- **The fix: "Ended —" on a note that had not begun.** `JournalEntryCard` decided a window had ended when the note did not matter today, so "no lunges from the 20th", written on the 10th, read "Ended — Matters from Sep 20". `windowEnded` (`mattering.ts`) is the one answer now: a range whose last day has gone by, or a one-off day that has passed — never an "always" note, a yearly day or a closed note. A past one-off day now reads "Ended —" too, as it already sat in Resolved. The change shows where the card lists notes that do not matter today: the profile's current Notes section (`NoteThreadCard` wraps this card), the dossier's journal rail and the session sheet's filed list. The briefing, the flags sheet and the critical strip use the same card but only ever show notes that matter today, so nothing changes there.

Tests: `record-selectors.test.ts` (every selector and branch, under `TZ=America/New_York`), eight new `windowEnded` cases in `mattering.test.ts`, `CriticalLine.render.test.tsx` (nothing when nothing is critical, whole sentences, the root thread opened, the count, the failed and partly-failed states, a 40px button) and `JournalEntryCard.render.test.tsx` (the window chip, with dates built from today). No Firestore, rules, index or Functions change.

**Carried to phase 9.** The critical line can point at an UNFILED critical note: `criticalEntries` includes it, but `notesOnRecord` keeps it in the To-file tray, not in `listed`. "Open the note" sends the shell to Notes with that thread id, and the Notes spec's focus handler does nothing for an id it does not list — the trainer would land on Notes with the note out of view. The Notes page's focus intent must scroll to the note's tray card when the thread is unfiled, with a `NotesPage.render` case for it.

## Phase 8 — the shell: seven pages, one Save bar, one load

**The long scroll is gone.** Notes & Profile is now `ClientCodex` (`src/features/client-codex/`): the sub-toggle's seven segments (Overview · Notes · FORD · Body & Pulse · Goals & Focus · Story · Account, AJ's order, wrapping rather than clipping), the critical line under it, the seven page panels and the one Save bar. `ClientInfoSheet.tsx`, `client-dossier/ClientDossier.tsx` and `client-dossier/ClientSnapshot.tsx` are deleted, and so is the nav's temporary `recordSection` shim (phase 3): the codex takes the page and card straight from the nav. Read the codex README's "The shell".

- **The pages, for now, are the old sections moved.** Each page is an adapter (`pages/*.tsx`) hosting the long scroll's sections exactly as they were, moved into `pages/legacy-blocks.tsx`: Notes = the composer and threads; FORD = Occupation and Recreation (the work and activity baselines) and the FORD hub; Body & Pulse = Build (height, wingspan, weight), Training story (Experience, **moved here from Life now** — AJ's decision 6), Watch-outs, InBody and the Pulse panel; Goals & Focus = the coach strategy, the why and the goal, each coach's focus, the team's plans and your jots, Mindbody's indexes; Story = a sentence saying it is not drawn yet; Account = the ID card, the Mindbody account notes, the contract with how they found us, and the Migration Hub. Each area rebuilds its page in its own phase.
- **The Overview is interim:** the Notes band (counts, the three loudest open notes, "Write a note", "All n notes") and one door per page, each with its segment's line. The full Overview is phase 18.
- **Mount on first visit, then stay.** The Overview mounts with the tab; every other page on its first visit, then stays mounted and hidden — so Goals' shared-notes and jot listeners and the Pulse panel's read happen only when those pages are opened, and switching back reads nothing. The record panel itself is kept mounted after the tab's first visit on a client, so coming back from Journey re-reads nothing and an unsaved edit is still there.
- **One load for the tab** (`useCodexData`): the journal, FORD, the InBody scans and your note dismissals, one listener each, handed to every page; the Pulse history is derived from the profile's progress-reports listener. **No FORD listener at all for a cross-train reader** (the database would refuse it). The render test counts them.
- **One Save bar.** "1 unsaved change · FORD · Occupation" — it says where, from any page; Show goes back to the card, Discard reverts, Save writes only the changed fields and `lastUpdatedBy` in one update (never `renewal`, never `undefined`). A refused save keeps every edit and says, for a permission refusal, where the record can be changed. The form re-seeds from the record only while nothing is unsaved (the old form's rule).
- **The wingspan bug** the plan's map found is fixed: the old form never seeded `wingspan`, so a saved wingspan showed as an empty box.
- **Recovery between sessions is gone from the screen** (decision 7). The field stays on every record; no control and no save can write it (`updateField` refuses a key that is not an editable field).
- **Read only for readers who may not edit** (`codexAccess`: the clients update rule — the home studio's team, the grant, administrators, Demo Mode). A cross-train studio sees the line "Read only here · Westlake keeps this record. Notes you write still save.", the record's fields locked, no Save bar and no Migration Hub. Before this, the long scroll offered them every editor and failed on Save. The form itself takes no edit from them either (`useRecordForm({ canEdit })`), so a control that ever escapes the lock cannot stage a change with no Save bar to save or discard it.
- **The watch-outs' injury rail says "none logged" only once the notes are read.** While they load it says nothing; when they failed it says "Injury and incident notes couldn't be loaded, so some may be missing." (the long scroll said "none logged" either way).
- **Coverage** is now `coverageOfClient` with the client's HOME studio's cutover (it was the active studio's, and ignored Mindbody's visit count), one value for Programming and the codex. The home is read as the rules read it (`recordStudioIdOf`: `homeStudioId`, else the older `studioId`). **The floor screens still use the iPad's studio** (the Active Session, the Clients list), so for a cross-train client Programming and the floor can word coverage differently until they move to the home too — a follow-up, not done here.
- **The progress-reports listener** moved into `useProgressReports` with its own render test (ready, failed, a client switch, out of quota) — phase 6's carried item. It now stays open while the record panel is mounted, so moving between Notes & Profile and the Activity Archive no longer re-subscribes.

**Typecheck baseline: 10 → 4.** Six of the ten pre-existing errors were in `ClientInfoSheet.tsx`, deleted here. The four left: `AppContent.tsx` ×2, `clinical-review/charts.tsx`, `EditTrainerModal.tsx`. No new error.

**Deviations from the plan, and why.**
- Experience moved to Body & Pulse in this phase (an additive `part` prop on `ActivityExperienceBaseline`), not in the FORD phase: `FIELD_HOME` puts the experience fields on Body & Pulse → Training story, and "Show" has to land on a real card from the first commit. The FORD phase still splits `LifeBaseline` into its three editors.
- `GoalsPanel` gained an optional `anchors` prop, so "Show" lands on "The why" and "Working toward" rather than the top of the page. Left out, it renders as before.
- Read only is interim: the record's editors sit in a disabled fieldset (`pages/RecordLock.tsx`) until each area's read views and Edit buttons replace them. Buttons inside a locked card (a contract's "show all", the goal history's "more") are disabled too for those readers.
- The shell spec's `stripEntries` / `noteZones` / kit `CriticalStrip` are not built: Notes' `CriticalLine` and `record-selectors` (phase 7) are the one version. The Story segment has no sub-line yet rather than a "since {year}" worked out without the whole story.
- `lastUpdatedBy` is written with `authTrainer.id`, as the old form did. Nothing reads it (pre-existing).

**Scale ratchet.** `CODEX_FILES` gains the shell's 17 files; `HOSTED_FILES` lists the moved sections and the pieces the pages host (19 files), whose off-scale text sizes are counted: **168**, the budget each area phase lowers. `BARE_PALETTE_BUDGET` 259 → 258 (the count).

**Tests.** `access.test.ts`, `record-form.test.ts`, `codex-data.test.ts`, `page-meta.test.ts`, `ClientCodex.render.test.tsx` (35 cases: pages and mounting, one listener each, none on FORD for a cross-trainer, no read on a switch, the critical line on five pages and its failed state, the Save bar's where / Show / Discard / Save / a refused save, a new snapshot of the record re-seeding only while nothing is unsaved (and a held form never writing back what it held), read only and a form that refuses edits for that reader, the watch-outs rail on a failed read, the Migration Hub door, nothing scrolled or focused while the tab is hidden, the neighbours and Done, a door scrolling to its card, no duplicate ids, no ResizeObserver or scrollIntoView), `useProgressReports.render.test.tsx`; `goals.render.test.tsx` now drives the panel with the real `useRecordForm`; the nav tests lose the shim. No Firestore, rules, index or Functions change.

**On an iPad (AJ):** at 744 and 834 portrait and in landscape, both themes — the bar wraps without clipping; an edit on FORD shows "1 unsaved change · FORD · Occupation" from Account, and Show goes back to it; sign in as a trainer from a cross-train studio and the pages are read only with no FORD; a saved wingspan shows on Body & Pulse → Build.

**Worth knowing.** `vite build` now warns that the `ClientProfileView` chunk is 511 kB (gzip 124 kB), over the 500 kB advisory; it was 496 kB before this phase, so the codex's +15 kB pushed it over. Advisory only; lazy-loading `ClientCodex` on the record tab is a cleanup-phase candidate. Pre-existing and unchanged: the clients update rule re-validates the merged document (`isValidClient`: first and last name non-empty and under 50 characters, `isActive` a boolean, `remainingSessions` a number), so an old record missing one of those refuses every Save with the plain "Couldn't save. Nothing was changed."

**Carried to phase 9.** The shell does not scroll to `note-{id}` or `notes-compose` (Notes' own anchors): the Overview's note rows, "Write a note" and the critical line's button open Notes at its top until the Notes page interprets them.

## Phase 9 — the Notes page

**Notes is built.** The Notes page is `src/features/client-notes/NotesPage.tsx`, inside the kit's Page (`client-codex/pages/NotesPage.tsx` is the adapter), on the tab's ONE load — it opens no listener of its own and has no fallback journal hook. `ClientJournalTab` loses its Notes area (it keeps Focus and Pulse until those pages are rebuilt). Read `src/features/client-notes/README.md` → "The Notes page". Top to bottom:

- **Banners** only when needed: the read guard rail, the missing index, and — new — **"Some of her notes couldn't be loaded, so what's below may be missing some."** A failed read never reads "No notes". No amber anywhere.
- **The composer is folded behind "Write a note…"** and drawn hidden when closed, so Close keeps the words ("Finish your note…"). The cursor goes into it only when it is opened, never on mount.
- **FORD / Life saves in place.** The same box and the same words become a FORD capture: no loudness or window, the four letters optional, "Save to FORD" → `clients/{id}/ford` only, stamped `fordStudioIdOf`. A FORD detail holds 2,000 characters (the rule) where a note holds 5,000, so a longer one says "FORD details hold up to 2,000 characters — this one is 2,001." and waits. A second tap on the chip is a note again, words intact. Offered to a reader who may change the record; a cross-train visitor is told where FORD is kept (the create rule would refuse them). The quick-note dialog gets the same mode, and stays open with "Saved to FORD".
- **A save that fails keeps the words** and says "Not saved — still here, try again". The composer used to keep them only by accident when a save failed; the page and the quick-note dialog now rethrow, and the composer catches.
- **The To-file tray** shows a loud note's Loudness pill: an unfiled critical note sits there, not in Open, and must still look critical.
- **One filter row**: search (every note, its updates and who wrote them — the coach chip row is gone), "All" and six category chips counting resolved notes too, always in the same places, and the **"Life · in FORD"** door, whose number shows only when it is known (none for a cross-train reader; none while loading).
- **Open** as full cards, loudest first, two columns from 720px wide (a 744pt portrait iPad gets one), a critical note across the whole width; **Standing context** one line each (the machine first and in full, the note's first line, who and when), opened in place; **Resolved** folded to "Resolved · N" and "Show the N resolved notes", then month by month, "closed Aug 7" / "ended Sep 20".
- **A critical note is drawn once**, in Open. "Critical & pinned" is gone; when a chip or a search hides a critical note, the critical line says so above the zones and "Show it" brings the card back.
- **The thread card is one panel**: kind (glyph and label, neutral ink), the machine, the Loudness pill — the only colour on the card (crimson Critical, plum Heads up) — the note, the spine of updates (open by default in Open), who and when (`threadCardMeta`), the **briefing line** (on your next briefing · hushed by you · from a date · off the briefing after three weeks), "Add an update" and a close whose words fit the note ("All healed up" only for an injury or incident; an equipment note simply closes). The ⋯ menu's **Archive takes the whole thread** — the root and every update in one batch — after an inline confirm; archiving the root alone used to leave its updates behind as notes of their own.
- **"No need to remind me" / "Show it again"** on the record, writing only `noteDismissals/{Auth uid}`, offered once the dismissals are read. The record never hides a note.
- **Doors land.** The critical line's "Open the note" and the Overview's rows open the thread (clearing a filter that hides it, opening its row, unfolding Resolved) and ring it for two seconds; **a critical note still in the To-file tray lands on its tray card** (phase 7's carried item); the Overview's "Write a note" opens the composer with the cursor in it; `notes-resolved` unfolds Resolved. Each is acted on once per move, and one made while the notes load waits for them (phase 8's carried item).
- **Settled life notes stay on Notes** in this phase; the FORD phase moves them into their pillars.

**Files.** New: `client-notes/NotesPage.tsx`, `ThreadRow.tsx`, `notes-intent.ts`, `notes-page.css` (+ `NotesPage.render.test.tsx`, `notes-intent.test.ts`). Rewritten: `NotesCatalog.tsx`, `NoteThreadCard.tsx`. Changed: `note-catalog.ts` (the filter is `{ category, search, showResolved }`; `NOTES_PAGE_CATEGORIES`; every zone holds every item and Resolved always has its months; `RESOLVED_PREVIEW` and `catalogCoaches` are gone), `threads.ts` (the Open and Standing lines), `record-selectors.ts` (`threadCardMeta`), `thread-write.ts` (`archiveThread`), `hooks/useClientJournal.ts` (`archiveJournalEntries`: one batch writing `isArchived` and `updatedAt` only), `components/journal/JournalComposer.tsx` (FORD in place, `onFordSaved`, keeps the words on a failed save, every small line 12px), `NoteSweep.tsx` (the pill), `QuickNoteDialog.tsx` (rethrows), `notes.css` (the old tiles, shelves and thread styles removed; `nc-hint`, the letters, the pill), `ford/ford-write.ts` (`FORD_BODY_MAX`), `client-profile/profile-nav.ts` (`threadIdOfAnchor`), `client-codex/ClientCodex.tsx` (hands Notes' anchors to the page), `components/journal/ClientJournalTab.tsx` (no Notes area), comments in `CriticalStrip.tsx` and `types/journal.ts` (on the record page a card's colour is its loudness only).

**No Firestore, rules, index or Functions change.** The writes are the ones the Notes area always made (a note, a FORD detail through `createFordEntry`, filing, close and reopen on the root, an update), plus `archiveJournalEntries` (`isArchived` + `updatedAt`, which the journalEntries update rule already allows) and the trainer's own dismissals document.

**Deviations from the plan, and why.**
- **"All" counts every thread the search finds**, not the six chips' sum: settled life notes are still on Notes in this phase (and a live life note stays there after the FORD phase), so a sum of six would count fewer notes than the zones show.
- **No window chip on the card's top row.** The mockup puts the window in the meta line ("Jess · Mar 15 · matters until Mar 22"); the spec had it in both. The meta carries it, and "ended Sep 20" once it ran out.
- **No `autoFocus` prop on the composer.** KNOWN-TRAPS forbids `autoFocus` inside a codex page (hidden pages mount too); the page focuses the box when it opens it.
- **The failed-read banner does not say "it retries on its own"** (the spec's copy): a failed listener is finished until the profile is opened again, so it says so.
- **The FORD line and the composer's FORD hint say "the home studio"**, not "people who can open her record": a cross-train trainer can open her record but not her FORD.
- **The FORD door's number is phase 7's `fordDoorCount`** (FORD's own count once read; the client document's counts while it loads; null when unknown or not this reader's) — no older-life-note count until the FORD page carries those notes.
- **The doors ride the shell's one navigation**, as INTEGRATION says: `ClientCodex` turns `note-{id}` / `notes-compose` / `notes-resolved` into a request keyed by the move (`notesIntentOf`), rather than separate `focusThreadId` / `composeOnOpen` props with handled-callbacks.
- **Composer and tray sizes.** They are shared with the session sheet and the post-session screen and keep their sizes there; on the page, `.nx-notes` rules bring each off-scale rule of `notes.css` onto the scale, and a new scale-test case fails if one is missing. The shared Loudness control keeps its 13 / 10px (one control everywhere) — the one exception on the page. `HOSTED_OFF_SCALE_BUDGET` stays 168: removing the Notes area moved no text size.

**Review fixes, before the commit.**
- **No chip counts when the notes could not be read**, as while they load: "All 0", "Injury 0" beside "couldn't be loaded" was the failed-read-is-empty mistake. The zone headers keep theirs — they count what is drawn under them, under the banner that says some may be missing.
- **A door to a composer already open puts the cursor back in it** (open on Notes, go to the Overview, tap "Write a note"): the focus effect keys on a tick that is new per open, not on the open flag.
- **A refused FORD save is tested** (the words stay, "Not saved — still here, try again", no journal write), and `saveToFord` catches a throw too, so no path clears the box or fails silently.
- **The quick-note dialog offers FORD in place only to a reader the create rule accepts** (`codexAccess(...).canEdit`, as the page); a cross-train trainer gets the hand-off there as well, where before they could type a detail and have it refused.
- **One "Show the N resolved notes", not two**: with nothing filtered, the empty line leaves it to the Resolved header's toggle.
- **A row's kind never breaks mid-word** in the 112px portrait column (`overflow-wrap: break-word` on its own span, not `anywhere`).
- **The composer's FORD hint says "the client's home studio"**, not the first name — the same words as its hand-off line.
- **Seen on the floor, and worth a look on the iPad:** the composer is shared with the Active Session notes sheet, so the sheet's small lines are now all 12px (they were 11, 11.5 and 12.5), and a refused save there now says "Not saved — still here, try again" beside the kept words. Nothing else in the sheet changes: FORD / Life still switches to "Remember this", and the draft still survives the sheet closing.

**Tests.** `NotesPage.render.test.tsx` (the zones, rows opening in place, Resolved and its months, chips counting resolved notes, the empty filter, the hidden critical note and "Show it", the door, no counts while loading, the four briefing lines and the hush writing only the trainer's dismissals, the tray's Critical pill and a door landing on it, doors to standing and resolved threads once per move, a door waiting for the notes, the composer and Resolved doors, a door refocusing a composer already open, one "Show the N resolved notes" when nothing is open, empty / migrating / failed (no chip counts) / large, FORD in place only for a writer), `NoteThreadCard.render.test.tsx` (close words, archive whole behind a confirm, the briefing line, no palette class), `notes.render.test.tsx` (now on `NotesPage`: FORD in place in the same box, the 2,000-character guard, the words kept when a note or a FORD detail is refused, and no Save to FORD in the quick-note dialog for a cross-train trainer), `note-catalog.test.ts`, `record-selectors.test.ts` (`threadCardMeta`), `notes-intent.test.ts`, `ClientCodex.render.test.tsx` (the critical line lands on the card; Write a note opens the composer; the door's number and FORD in place for the home studio; neither for a cross-train reader, whose note still saves), and the scale test's override case. Suite **271 files / 4,165 tests** (was 269 / 4,120). Typecheck **4** (the phase-8 baseline). Build passes (`ClientProfileView` 501 kB, down from 511). Case check clean.

**On an iPad (AJ):** at 744 and 834 portrait and in landscape, both themes — Notes opens with the bar, not the composer; a critical note shows once, full width; a Standing row opens in place and folds again; "Show the N resolved notes" works; picking Equipment on a client with a critical injury note shows the red line with "Show it"; typing, then FORD / Life, keeps the words and "Save to FORD" lands the detail on FORD; from Account, the critical line's "Open the note" rings the card on Notes; the Overview's "Write a note" opens the composer with the keyboard up; signed in as a cross-train trainer — no FORD number, no Save to FORD (on the page or in the header's Note dialog), and a note still saves. In an Active Session, open the notes sheet: the small lines read at one size and nothing else has moved. At 744 and 820 portrait, a Standing row's kind ("PREFERENCE", "COACHING TIP") wraps at a space, never mid-word.

## Phase 10 — the FORD page (without the new fields)

**FORD is built.** The FORD page is `src/features/ford/page/FordPage.tsx` (`client-codex/pages/FordPage.tsx` is the adapter), on the tab's ONE load — it has no hook of its own, so it cannot open a listener. It replaced the Life section's hub (`FordSection`, deleted) and the work and activity baselines above it. Read `src/features/ford/README.md` → "The FORD page". Top to bottom, as the mockup has it:

- **Coming up** leads with the **birthday Mindbody holds** — "Her 69th birthday · In 17 days · Family · from Mindbody" — rolled forward on the studio's day through the same `daysUntilBirthday` Relay's Mine uses, beside FORD's dated details (`coming-up.ts`). The when is coloured by urgency (`urgencyOf`: this week FORD's "now" orange, this month "soon" blue), never by pillar. An annual FORD "Birthday" on the same day folds into the birthday's row and its gesture rides on it; a legacy `client.events` birthday is dropped the same way but not linked. Tapping a birthday nobody planned for opens a new annual Family "Birthday" detail with the gesture open. Three cards, then "All N coming up".
- **To file**: each capture says where it was caught and by whom ("Caught mid-session by Jess Moreno, Mar 15"), and four 40px buttons carry the letter AND the word — no more letter-only buttons with a tooltip.
- **Four pillar cards**, every one built the same way: head (mark, name, FORD's own blurb, Edit / Done where the band is record fields, Add), the **band**, every standing fact, FORD's moments merged with the **older life notes** filed there (three, then Show all), the **Pulse lines**, a gap line, and **Ask next**. No count chip on the head.
  - **Occupation is the Work block and Recreation is Active outside the studio.** Edit opens the editor in the band; its fields go through the codex's one form, so Done only closes it, the band says "Not saved yet", and the Save bar says "FORD · Occupation". The job title is **free text with suggestions** (a `<datalist>` of the old list's 19 titles; `OccupationSelect`, with its raw hex and always-dark popover, is deleted) and Retired is two picks, Working and Retired. Family is the emergency contact and the birthday from the record; Dreams quotes the why from Goals & Focus in full, with a door there.
  - **Older life notes move here from Notes.** `notesOfJournal` passes `lifeOnFord`, so a settled journal `life` note (standing or resolved) leaves Notes' zones and counts and shows in its pillar, "From an older note · Jess Moreno · Sep 20, 2025" — Birthday and Anniversary under Family, Vacation under Recreation, the rest in an "Older life notes" card below the pillars. A live one stays on Notes. They are journal notes, so a cross-train reader who is refused FORD still sees them. A note with updates keeps them one tap away ("Show 2 updates"), because it can no longer be opened on Notes.
  - **Pulse lines** (`pulse-links.ts`, no Firestore, no writes) quote the Pulse beside the pillar it is about, in the Dial's own words — "Pulse stress, “Caring for someone”: Moderate on Dec 9, Mild on Mar 10." under Family, work and retirement under Occupation, "I stay physically active outside of workouts." under Recreation — with a footnote that nothing is copied between FORD and the Pulse.
  - **Ask next knows about retirement** (`ask-next.ts`, `FORD_PROMPT_WHEN`): a retired client is never asked "How is work treating you?" ("How is retirement going?" — "The work questions are skipped because she is retired"), a working one never how retirement is going. **The briefing cue passes the same context** — the one floor-visible change of this phase.
- **Going above and beyond**: Idea → Planned → Done with "I'll do it" (the signed-in person, by Auth uid) and "Mark done" ("What actually happened? Worth a sentence."), through `setGestureStatus`, the Delight queue's own writer. Owners' names in full.
- **The sub-toggle's FORD line is FORD's own** (`fordSubnavLine`): "birthday in 17 days", else "a date in 12 days", else "2 to file", else "6 details", else "nothing on file yet". The count — on the sub-toggle and on Notes' "Life · in FORD" door — is FORD's details **plus the older life notes** (`olderLifeCountOf`), null while the journal loads.

**The dialog.** `FordDetailDialog` **stays open with every word when a save is refused** ("Not saved — still here, try again"); the hub closed it and ignored the failed create, so the sentence was lost. **A new idea is unowned**: the dialog used to make whoever typed an idea its owner, so the Delight queue's "Needs an owner" was never true for one; now an idea has no owner until someone says "I'll do it", an existing owner is kept, and one moved to Planned or Done with nobody on it is owned by whoever moved it. The title is 17px and its icon buttons 40px. New props `initial`, `openGesture` and `bodyPlaceholder` serve the birthday and "Add an idea".

**The life editors.** `LifeBaseline.tsx` is now `WorkEditor`, `RecreationEditor` and `ExperienceEditor`, built on the kit's pick pills through `client-life/controls.tsx` (`PickGroup`, `ChipPicks` — the old `Seg` and `ChipSet`, same behaviour: tap the chosen pill to clear it, custom chips up to 40 characters, 24 at most, a 40px Add). `ExperienceEditor` is Body & Pulse → Training story in the interim Body host. `client-life.css` imports its tokens and is on the codex scale.

**Also.** `DelightQueue`'s empty line and the Operations → Delight queue intro say where a gesture is made now (a client's FORD page), not the Life section's gift button. `ford.css` loses the hub's rules (the briefing row, the queue banner, the dialog's buttons, the gesture chip and the notice stay). The kit's `TextInput` gains an optional `onBlur` (the job title trims on blur).

**Files.** New: `ford/ask-next.ts`, `coming-up.ts`, `pulse-links.ts`, `page-model.ts` (each with a `.test.ts`), `ford/page/` (`FordPage`, `PillarCard`, `bands`, `ComingUp`, `UnfiledTray`, `AskNextLine`, `AboveAndBeyond`, `ford-page.css`, `FordPage.render.test.tsx`), `client-life/controls.tsx`. Rewritten: `client-life/LifeBaseline.tsx`, `client-life.css`, `client-codex/pages/FordPage.tsx`. Changed: `ford/types.ts` (`FORD_PROMPT_WHEN`), `ui.tsx` (`pillarPrompt` takes the context), `FordDetailDialog.tsx`, `FordBriefingCue.tsx`, `DelightQueue.tsx`, `ford.css`, `client-life/life.ts` (`isRetiredClient`, `activitySentence`, `recreationSentence`, `OCCUPATION_SUGGESTIONS`), `client-codex/codex-data.ts` (`lifeOnFord`, `olderLifeCountOf`, `fordCountOf`), `ClientCodex.tsx` and `page-meta.ts` (FORD's line), `pages/NotesPage.tsx` and `record-selectors.ts` (the door counts the older notes; `olderLifeCount: null` is unknown), `pages/BodyPage.tsx` (`ExperienceEditor`), `kit/fields.tsx`, `admin/AdminDashboardView.tsx` (copy). Deleted: `ford/FordSection.tsx`, `components/OccupationSelect.tsx`.

**No Firestore, rules, index or Functions change.** The writes are FORD's existing writers (`createFordEntry`, `updateFordEntry`, `tagFordEntry`, `archiveFordEntry`, `setGestureStatus`) and the record form's one `updateDoc`. No rules test changed, so this phase needs no `test:rules` run.

**Deviations from the plan, and why.**
- **"In one line" and "Follow up next time" are not here** — phase 11, as INTEGRATION splits them. Ask next is always one of FORD's prompts for now; `ask-next.ts`'s `AskNext` has only its `prompt` kind.
- **One selector each, as INTEGRATION rules**: the older notes are Notes' `olderLifeNotesByPillar` (no FORD-side `olderLifeNotes`), the count is `fordDoorCount` with `olderLifeCount` (no `fordDetailCount`), "where" is `FIELD_HOME` (no `FORD_BAND_WHERE`), the read state is phase 1's `status` (no `readState` / `readError`), and the Pulse is the shell's one history (not `historyFromDocs(progressReports, length + 1)`, which would always claim completeness).
- **A failed FORD read still lets a trainer add** (phase 1's `fordCanAdd`: never block a save); the area spec disabled writes whenever the read was unknown. A refused read, or a client with no studio, offers no add.
- **The Pulse stress lines use the two newest rounds that said anything about stress**, not simply the two newest rounds: a Pulse is filled a piece at a time, and "not raised on Mar 10" about a round that never asked would read as an answer. A statement line uses the two newest rounds that answered it.
- **The kit's `FordMark`** (22 and 40) and **the kit's `inTime`** are used rather than a `letterSize` on `ui.tsx`'s mark and a `fromNowLabel` of FORD's own — one mark, one way of saying a date.
- **Single-choice picks are the kit's toggle pills (`aria-pressed`)**, not `role="radio"`, so the life editors behave like every other codex editor.
- **The sub-toggle line is lower case and plain** ("birthday in 17 days"), like every other segment's and the mockup's; a FORD date is "a date in 12 days" (a detail's `subject` is usually a person's name). `ProfileSubnav` has no meta colour, so the line is not coloured by urgency — it is never coloured by pillar either.
- **The bands' "Body & Pulse reads this line too" sources are left out** until Body & Pulse shows the line (phase 12) — a claim the screen cannot yet keep.
- **No intake slot on the Occupation and Recreation bands**: INTEGRATION puts the Mindbody intake offers on Account only (phase 17), so the same line cannot be added twice from two pages.
- **The hub's CSS goes now**, not in the cleanup phase: the component it styled is deleted here. `HOSTED_OFF_SCALE_BUDGET` falls from 168 to 148 (the FORD hub gone, the life editors on the codex list).
- **The "habits to unteach" switch stays a switch** (ExperienceEditor moved as it was); only Retired became picks.

**Review fixes, before the commit.**
- **The birthday card plans nothing until FORD has answered.** While FORD loads, or after the read failed, the client's own "Birthday" detail may exist unseen, and tapping the card would have opened a second one; until then an unplanned birthday card is read only (a linked one still opens).
- **The FORD count never turns a failed read into "none".** When the journal's notes could not be read and FORD holds nothing, the sub-toggle says "couldn't load" (not "nothing on file yet") and the Notes door shows no number (not "0"); FORD's own details are still counted when there are some. One helper, `fordCountOf` in `codex-data.ts`, feeds both.
- **A chip tapped on and off leaves nothing unsaved.** `ChipPicks` keeps the order the list was saved in (a new pick goes on the end) instead of the pills' order, so the Save bar no longer shows a phantom change for Recreation or Before Max Strength, and the recreation line reads as it was entered.
- **Tests for what had none:** the briefing cue for a retired and a working client with nothing under Occupation (mounted, not only the pure rotation), a refused FORD read for a trainer who may edit the record, and the birthday card while FORD loads or failed.
- **Two stale lines** said a gesture's outcome shows on "the client's Life section" (TESTING-CHECKLIST) and that `ford/` is "the Life section of the record" (`src/features/README.md`); both now name the FORD page.

**Tests.** `ask-next.test.ts` (the drift guard; 60 days of rotation for a retired and a working client; no context is today's rotation), `coming-up.test.ts` (17 days / 69th / soon; today; yesterday rolls to next year; bad dates; urgency thresholds; the linked and the legacy birthday; sorting; `ordinal`), `pulse-links.test.ts`, `page-model.test.ts`, `life.test.ts` (the four new helpers), `FordPage.render.test.tsx` (Coming up and planning a birthday, the tray's labelled buttons, Show all, an older Anniversary in Family, retirement-aware Ask next, the Pulse's own words, Edit → "Not saved yet" with no write, read-only readers, the four read states never saying "Nothing on file yet", the dialog keeping the words on a refused save, owners kept and ideas unowned, I'll do it and Mark done, every button 40px, no clipping), `LifeBody.render.test.tsx` (every old `updateField` assertion kept, now through `OccupationBand`, `ExperienceEditor` and `RecreationEditor`, plus the free-text title and its datalist), `ClientCodex.render.test.tsx` (the Save bar cases now Edit then pick Retired; an older Anniversary note is on FORD's Family card and off Notes, counted on the door and the sub-toggle, and still shown to a cross-train reader), `codex-data.test.ts`, `record-selectors.test.ts`, `page-meta.test.ts`, and `useClientFord.render.test.tsx` (the Life section's cases moved to the FORD page's test). Suite **276 files / 4,259 tests** after the review fixes (was 271 / 4,165). Typecheck **4** (the phase-8 baseline). Build passes (`ClientProfileView` 517 kB, up from 501 — the FORD page). Case check clean.

**On an iPad (AJ):** signed in as a Life Transformer, at 744 and 834 portrait and in landscape, both themes — FORD opens with Coming up (her birthday, "In N days", the when orange only this week); the pillars are one column in portrait below the 13-inch and two from about 880pt (the 13-inch portrait and landscape); nothing is cut off. On Occupation, Edit → type a job title that is not on the list, pick Retired → "Not saved yet", Ask next changes, the Save bar says "FORD · Occupation"; Save, and the band reads "Retired — was …". A client with an old "Anniversary" note in the journal: it is in Family, "From an older note", and gone from Notes. File a capture from To file. "I'll do it" on an idea puts your name on it. Remember something → save → "Saved to FORD." Signed in as a trainer from another studio: the notice that FORD is kept by the home studio, the older notes still there, no Add, no Edit. On the briefing, for a retired client with nothing on file under Occupation, the FORD line never asks about work.
