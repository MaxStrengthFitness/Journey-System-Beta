# The Client Codex — Notes & Profile as seven pages

*Sep 24 2026, overnight. Branch `client-codex`, one commit per phase. **Nothing is pushed or deployed** until AJ says go: the phases accumulate on the branch (AJ's decision). The plan is `INTEGRATION.md` and the six area specs in the session scratchpad; the approved mockup is `client-codex.html`. This document is started in phase 2 and finished in the last phase (the full story, the baselines, how it ships, and the index and CHANGELOG rows).*

## Phases so far

- **1 — FORD reads that work for every trainer, and honest test commands** (`29f653d`). `useClientFord` listed FORD with no studio filter, so the rules refused it for everyone below franchise owner and the refusal read as "nothing on file". Now filtered on the client's studio, with a `status` that tells "couldn't be read" and "kept by the home studio" from empty. `useClientJournal` reports what it could read and hands out the sessions it already streams. The test commands skip the `.claude` worktree copies. See KNOWN-TRAPS → The client profile.
- **2 — The InBody normal variation (AJ's decision 8)** (`9e77640`). Below.
- **3 — Navigation: the record tab becomes pages** (`b02e66d`). The record arm of the profile's one location is `{ page, anchor }`; every old section and tab id still lands; the tab always opens on the Overview. See `src/features/client-profile/README.md`.
- **4 — The sub-toggle can wrap** (`5884295`). `ProfileSubnav` gains `wrap`, `idPrefix` and a plum `warn` dot, all opt-in, so Programming and the Activity Archive are unchanged. See KNOWN-TRAPS → The client profile.
- **5 — One visual kit** (`c8203b9`). Below.
- **6 — Additive props and exports the pages need** (`b751b9f`). Below.
- **7 — Thread selectors and the critical line** (`f52a748`). Below.
- **8 — The shell: seven pages, one Save bar, one load** (`cfa13f7`). Below.
- **9 — The Notes page** (`1a25a71`). Below.
- **10 — The FORD page (without the new fields)** (`4682995`). Below.
- **11 — FORD's new fields: In one line and Follow up next time (AJ's decision 3)** (`4696f96`). Below.
- **12 — Body & Pulse: build, where it matters, watch-outs, floor, measured and told, Pulse** (`45c53bb`). Below.
- **13 — Body & Pulse: Over time, how she arrives and leaves (AJ's decision 9)** (`9014293`). Below.
- **14 — Goals & Focus** (`b49c3b2`). Below.
- **15 — Story** (`36ae64c`). Below.
- **16 — Account** (`b49fc92`). Below.
- **17 — The Mindbody intake-notes matcher (AJ's decision 4).** Below.

## Phase 2 — the scanner's normal variation

An InBody scan of the same body reads a little differently every time, and until now every screen called any difference a change: "+1.2 lb muscle" was proof on the renewals pipeline, made a client an upgrade candidate, and printed green on the progress report the client takes home.

**The rule** (`src/features/inbody/variation.ts`): a change smaller than the client's HOME studio's number is "within the scanner's normal variation" and no screen calls it up, down, better or worse; a change equal to the number or bigger is called. The number itself is always shown. Max Strength's defaults are **3.5 lb** skeletal muscle, **5.3 lb** body fat mass and **2.7 points** body fat %. Fat mass and body fat are the cautious end of McLester et al. 2020 (checked against the abstract tonight: 2.12–2.73 %, 1.49–2.39 kg fat mass, 1.60–2.32 kg fat-free mass). The study did not measure skeletal muscle: 3.5 lb is AJ's figure, the low end of the fat-free-mass range. Weight has none and is never toned.

**Each studio sets its own** on My Studio → Studio → *InBody: the scanner's normal variation*, between the studio's day and Renewals. It is stored at `studios/{id}.inbodyVariation`, written whole with `updatedBy` = the Auth uid; "Use Max Strength's defaults" removes the field. **No rules change**: the studio update rule already scopes the write to the studio's own leaders (and the grant), franchise owners and administrators — pinned by four new rules tests ("Studio InBody variation"). No index change. No read: the studios are already in memory.

**What is stored never changes.** `clients/{id}.inbodySummary` and the nightly `renewal.proof.inbody` keep the raw changes, and the nightly job is untouched. Lowering a studio's numbers brings a hidden change straight back.

### The wording, before and after

At the defaults, for the fixtures the tests pin (a client up 2.3 lb of muscle and down 1.8–2.2 points of body fat):

| Where | Before | After |
| --- | --- | --- |
| **Progress report** — Body Composition, Change column | `+2.3 lb` (green), `−2.2 pts` (green) | `+2.3 lb · within normal variation`, `−2.2 pts · within normal variation` (uncoloured), and one line under the table: *"Within normal variation: smaller than the difference an InBody scanner can show between two scans of the same body, so it isn't counted as a change."* A change beyond the variation prints as before, e.g. `+4.1 lb` (green) |
| **Renewal Brief** — health, the InBody line | `InBody since Jan 15: muscle up 2.3 lb, body fat down 1.8 points` | `InBody since Jan 15: no change bigger than the scanner's normal variation`. Only a called change is named: `InBody since Jan 15: muscle up 4 lb, body fat down 3.1 points`, or just `… muscle up 2.3 lb` when a studio's muscle number is 2 |
| **Renewal Brief** — upgrade reasons | `InBody: muscle up 1.2 lb.` counted as visible progress | only muscle up, or body fat down, beyond the variation counts; otherwise "Progress isn't visible in the data yet." |
| **Pipeline row and the profile's renewal card** — the proof line | `In 11 of the last 12 weeks · stronger on 12 of 14 machines · +2.3 lb muscle` | `In 11 of the last 12 weeks · stronger on 12 of 14 machines` (the muscle part returns at `+4 lb muscle` and above) |
| **Pipeline** — "Upgrade candidates" filter | a client whose only progress was +1.2 lb of muscle was a candidate | not a candidate at the defaults; each row is judged by its client's home studio's numbers |
| **InBody card** — the sentence | `Since Jan 15: muscle up 2.3 lb, body fat down 2.2 points.` | `Since Jan 15: no change bigger than the scanner's normal variation.` — or `Since Jan 15: muscle up 4.0 lb; body fat within the scanner's normal variation.` when one is called |
| **InBody card** — the tiles | `+2.3 lb` (green) | `+2.3 lb · within normal variation` (uncoloured) |

### Files

`src/features/inbody/variation.ts` (+ test), `useInBodyVariation.ts`, `scans.ts` (`formatCalledChange`; `changeTone` and `summarySentence` take the variation), `InBodyCard.tsx` (+ render test), `InBodyReportSection.tsx`; `src/components/ClientProgressReportView.tsx`; `src/features/renewals/brief.ts`, `sentences.ts`, `options.ts`, `pipeline.ts`, `RenewalCardDialog.tsx` (+ the three tests); `src/features/admin/renewals/RenewalsPipeline.tsx`, `RenewalBrief.tsx`; `src/features/my-studio/InBodyVariationPanel.tsx` (+ render test), `StudioSection.tsx`; `src/types.ts` (`Studio.inbodyVariation`); `tests/firestore.rules.test.ts`; the InBody and My Studio READMEs, KNOWN-TRAPS and ARCHITECTURE.

## Phase 5 — one visual kit

Every codex page is built from one kit, so a trainer learns one panel, one button and one set of words: `src/features/client-codex/kit/` (read `src/features/client-codex/README.md`). Nothing on screen changes in this phase — the kit has no caller yet; the shell (phase 8) and the pages use it.

- **Tokens** (`codex.tokens.css`): every colour is an alias of a token the app already has (`--eq-*`, `--ford-*`), so there is no new palette and light and dark come for free. Text is 11 / 12 / 14 / 17 / 30px and nothing else.
- **Pieces** (`primitives.tsx`, `ReadEdit.tsx`, `fields.tsx`, `SaveBar.tsx`, `kit.css`): Page (head, ‹ › neighbours, Next card), Card, Slot, FactList, Rows, Chip, LoudChip, Btn, FordMark, the read-view-then-Edit frame, the inputs and pick pills, and the one Save bar ("1 unsaved change · FORD · Occupation"). Nothing tappable is under 40px; nothing is clipped; solid buttons are brand blue, never orange.
- **Words** (`text.ts`, `src/lib/first-sentences.ts`, `pronouns.ts`): whole sentences instead of "…" (a critical note is never cut mid-instruction), dates the studio way, and she / he / they from the gender Mindbody holds — they when it holds none (the overnight default for AJ's pronoun question; one file to change). The review caught `firstSentences` ending "Train her at 7 a.m. only…" after "a.m." and "Ask "does it pinch?" before every set" after the question mark; the word after the mark now decides (a lower-case word or a number means the sentence goes on), so an abbreviation it does not know makes the line longer instead of cutting it. `inTime` counts days up to 30 ("Birthday in 17 days", as the mockup says), and FORD will use it rather than a second formatter.
- **Loudness**: `LOUDNESS_TONE` is exported from `src/features/rating/Loudness.tsx`, so the codex shows Heads up plum and Critical crimson with the one control's own mapping (a departure from the mockup's gold, as agreed).
- **The contract is a test** (`kit/scale.test.ts`): it reads every codex file and fails on an off-scale text size, a raw hex colour or a colour a component sets itself, clipped text (a line clamp is allowed only on the two named row-body selectors, at two lines), a regex lookbehind or a raw invisible character, and on any file in the folder that is not on its list. Adding a 13px rule to `kit.css` fails it (checked). `kit/kit.render.test.tsx` mounts every piece in light and dark and checks each control is at least 40px tall.
- **Accessibility**: a field's label is its name and its hint its description, so VoiceOver reads each once; the Save bar announces the first unsaved change through a live region that is mounted before it.

No Firestore, rules, index or Functions change.

## Phase 6 — additive props and exports the pages need

The codex loads each thing ONCE for the whole tab and hands it to every page. This phase lets the existing pieces take what the tab loaded instead of reading it again, and exports the small readers the pages need. **No screen changes**: every new prop is optional, and a caller that passes none of them behaves exactly as before (the existing notes, Pulse panel, goals and contract tests pass unchanged).

- **Shared data, one listener each.** `FordSection` takes `ford` (the tab's FORD stream), `InBodyCard` takes `inbody` (the tab's scans), and `ClientCheckInPanel` takes `draft` (the Body & Pulse page's one Pulse draft). Given one, each disables its own hook — it is not opened a second time. For the Pulse this is more than cost: two drafts of one client autosaving side by side is the duplicate-draft bug. The panel also takes `startInClientMode` (opens client mode once the draft is in, and again each time it turns true, so "Hand to client" on the page is one tap) and `onClientModeClose`; client mode never opens over a draft still loading. `CheckInDraftState` is exported.
- **Readers exported, not rewritten.** `statementAnswer` (a saved round's own answer to one statement on 0–10 — a v1 answer converted, `null` when the round did not ask; it was the private `ownAnswer`) and `painKeyOf` (`region:side`, how a spot is paired with last round's). `ContractTermRow.cancelledAt` — when Mindbody said a contract was cancelled; null unless the row is cancelled (a re-activated contract with a stale stamp never reads as cancelled) and for paid-in-full rows. Story will read it.
- **`useFocusActions`** (`src/features/goals/`): set, achieve, extend, retire and check in on a focus, moved out of `ClientJournalTab` unchanged (author = the Auth uid, the check-in carries its focus id). The journal's Focus area uses it now; Goals & Focus will.
- **`useNoteDismissalsState`**: the trainer's dismissals plus whether they have been read (`loading · ready · failed`), keyed by uid so another trainer never inherits the map. The Notes page offers hush / restore only once it is `ready`. `useNoteDismissals` (the briefing's) returns the same map as before.
- **ClientProfileView** gains `progressReportsStatus` (whether the progress-reports listener answered for THIS client — Pulse history is read from that list, and a list never read must not pass for "no Pulse on file") and `journeyCompletedCount` (the Journey session count, null until the count answers — the header's number starts at 0). The profile is not remounted per client, so both are stamped with the client they are for and read through `answerFor` (`src/features/client-profile/client-answer.ts`). Nothing reads them yet; the shell (phase 8) passes them to the codex. Out of quota, the reports listener never opens, so the status says `failed` rather than waiting forever.

Tests: the new props each have a render case (the panel with the page's draft makes no read of its own — checked by count against the panel alone — and writes once; client mode waits for the draft and re-opens on a second tap; FORD and InBody open no listener when handed their data and say "couldn't be read" / "loading" rather than "nothing"); `useFocusActions` mounts on its own and through the journal's Focus card; the dismissals status, `answerFor`, `cancelledAt`, `statementAnswer` (v1 and v2) and `painKeyOf` have unit cases. No Firestore, rules, index or Functions change.

**Carried to phase 8.** Nothing mounts `ClientProfileView`, so the wiring that stamps `progressReportsStatus` (the listener's success and error callbacks) has no test: dropping the `failed` stamp would leave Pulse history "loading" forever with the suite still green. Phase 8 moves the reports listener into a small hook with its own render test (ready, failed, a client switch, out of quota). The codex also stays keyed on `client.id`: `draft`, `ford` and `inbody` are not checked against the panel's `client`, so a stale hand-over is prevented only by that key.

## Phase 7 — thread selectors and the critical line

Five of the seven pages talk about notes, and each area's spec had written its own version of the same selection — one "how to coach her" pulled in every legacy session wrap-up and labelled it a Preference; one "her notes on this machine" dropped every standing set-up note. Each selection now lives ONCE, in `src/features/client-notes/record-selectors.ts`, and every page will read the tab's one journal load through it. **No screen changes yet except one fix** (below): the shell (phase 8) and the pages use these.

- **What Notes lists** — `notesOnRecord`: the five profile fields the record edits elsewhere stay off, an unfiled note goes to the To-file tray, and a life note that is no longer live is marked *settled* (FORD's to show, "from an older note"). Settled life notes **stay on Notes until the FORD page carries them** (`lifeOnFord`, default false; the FORD phase turns it on), so no note ever leaves the tab in between. A note is settled only if `olderLifeNotesByPillar` will place it — one test, `isOlderLifeNote`, for both — so a `client.events` copy and an update whose root is out of the load (an orphan, which stands as a thread of its own) stay on Notes rather than falling between the two pages; a test asserts the two selections are the same set.
- **Counts** — `notesSummary` (takes `notesOnRecord`'s result whole, so the To-file tray is counted; critical is the briefing's own count), `notesTabMeta` for the sub-toggle ("3 open · 1 critical"; "2 to file" when the only notes wait for a category; nothing while loading; "couldn't load", never "none yet", when the read failed), `notesSummarySentence` for the Overview ("… · 2 to file"; "No notes in Journey yet" only when the tray is empty too).
- **One thread in words** — `whoOf` ("Jess"; the source for an import), `shortDay`, `threadRowMeta` ("Jess · Nov 3 · closed Nov 7", "· ended Oct 3" when the window ran out), `closeWordsOf` ("All healed up" / "It's back" for an injury or incident only — an equipment note simply closes), `briefingStatusOf` (on the briefing, hushed by you, from a date, or gone quiet after three weeks; "on" is marked unchecked until the trainer's dismissals are read).
- **The other pages** — `howToCoachThreads` (Preference and Coaching-tip notes by KIND, so legacy wrap-ups stay out; focus check-ins stay on their focus), `injuryThreads` (by the catalog's category, optionally with incidents; the medical fields are left to Body), `threadsByMachine` (every open or standing note on a machine, loudest first — no focus check-ins, which would bury the set-up notes), `olderLifeNotesByPillar` (Birthday and Anniversary → Family, Vacation → Recreation; the `client.events` rows FORD already draws are left out), `fordDoorCount` (the "Life · in FORD" door: FORD's own count once read; while it loads, the client document's counts — never its text — plus the `client.events` rows FORD adds, so the number does not jump when FORD arrives; **null, not 0,** when unknown or when the reader can't open FORD), `hiddenCriticalThreads`.
- **The critical line** (`CriticalLine.tsx`, `critical-line.css`): "Critical · Leg Press: stop at 90° at the bottom turn." — the briefing's selection, ignoring dismissals (the record never hides a note), the machine named in full, and **whole sentences, never cut and never "…"** (`firstSentences(body, 90)`; the line wraps). "· and 2 more critical notes" when there are more. With `failed` it says *"Notes couldn't be loaded, so a critical note may be missing."* instead of drawing nothing; that state is neutral, not crimson, because it is not a critical note. It imports nothing from the codex; its three files are on the scale test's list (checked: a 13px rule in `critical-line.css` fails it).
- **The fix: "Ended —" on a note that had not begun.** `JournalEntryCard` decided a window had ended when the note did not matter today, so "no lunges from the 20th", written on the 10th, read "Ended — Matters from Sep 20". `windowEnded` (`mattering.ts`) is the one answer now: a range whose last day has gone by, or a one-off day that has passed — never an "always" note, a yearly day or a closed note. A past one-off day now reads "Ended —" too, as it already sat in Resolved. The change shows where the card lists notes that do not matter today: the profile's current Notes section (`NoteThreadCard` wraps this card), the dossier's journal rail and the session sheet's filed list. The briefing, the flags sheet and the critical strip use the same card but only ever show notes that matter today, so nothing changes there.

Tests: `record-selectors.test.ts` (every selector and branch, under `TZ=America/New_York`), eight new `windowEnded` cases in `mattering.test.ts`, `CriticalLine.render.test.tsx` (nothing when nothing is critical, whole sentences, the root thread opened, the count, the failed and partly-failed states, a 40px button) and `JournalEntryCard.render.test.tsx` (the window chip, with dates built from today). No Firestore, rules, index or Functions change.

**Carried to phase 9.** The critical line can point at an UNFILED critical note: `criticalEntries` includes it, but `notesOnRecord` keeps it in the To-file tray, not in `listed`. "Open the note" sends the shell to Notes with that thread id, and the Notes spec's focus handler does nothing for an id it does not list — the trainer would land on Notes with the note out of view. The Notes page's focus intent must scroll to the note's tray card when the thread is unfiled, with a `NotesPage.render` case for it.

## Phase 8 — the shell: seven pages, one Save bar, one load

**The long scroll is gone.** Notes & Profile is now `ClientCodex` (`src/features/client-codex/`): the sub-toggle's seven segments (Overview · Notes · FORD · Body & Pulse · Goals & Focus · Story · Account, AJ's order, wrapping rather than clipping), the critical line under it, the seven page panels and the one Save bar. `ClientInfoSheet.tsx`, `client-dossier/ClientDossier.tsx` and `client-dossier/ClientSnapshot.tsx` are deleted, and so is the nav's temporary `recordSection` shim (phase 3): the codex takes the page and card straight from the nav. Read the codex README's "The shell".

- **The pages, for now, are the old sections moved.** Each page is an adapter (`pages/*.tsx`) hosting the long scroll's sections exactly as they were, moved into `pages/legacy-blocks.tsx`: Notes = the composer and threads; FORD = Occupation and Recreation (the work and activity baselines) and the FORD hub; Body & Pulse = Build (height, wingspan, weight), Training story (Experience, **moved here from Life now** — AJ's decision 6), Watch-outs, InBody and the Pulse panel; Goals & Focus = the coach strategy, the why and the goal, each coach's focus, the team's plans and your jots, Mindbody's indexes; Story = a sentence saying it is not drawn yet; Account = the ID card, the Mindbody account notes, the contract with how they found us, and the Migration Hub. Each area rebuilds its page in its own phase.
- **The Overview is interim:** the Notes band (counts, the three loudest open notes, "Write a note", "All n notes") and one door per page, each with its segment's line. The full Overview is phase 18.
- **Mount on first visit, then stay.** The Overview mounts with the tab; every other page on its first visit, then stays mounted and hidden — so Goals' shared-notes and jot listeners and the Pulse panel's read happen only when those pages are opened, and switching back reads nothing. The record panel itself is kept mounted after the tab's first visit on a client, so coming back from Journey re-reads nothing and an unsaved edit is still there.
- **One load for the tab** (`useCodexData`): the journal, FORD, the InBody scans and your note dismissals, one listener each, handed to every page; the Pulse history is derived from the profile's progress-reports listener. **No FORD listener at all for a cross-train reader** (the database would refuse it). The render test counts them.
- **One Save bar.** "1 unsaved change · FORD · Occupation" — it says where, from any page; Show goes back to the card, Discard reverts, Save writes only the changed fields and `lastUpdatedBy` in one update (never `renewal`, never `undefined`). A refused save keeps every edit and says, for a permission refusal, where the record can be changed. The form re-seeds from the record only while nothing is unsaved (the old form's rule).
- **The wingspan bug** the plan's map found is fixed: the old form never seeded `wingspan`, so a saved wingspan showed as an empty box.
- **Recovery between sessions is gone from the screen** (decision 7). The field stays on every record; no control and no save can write it (`updateField` refuses a key that is not an editable field).
- **Read only for readers who may not edit** (`codexAccess`: the clients update rule — the home studio's team, the grant, administrators, Demo Mode). A cross-train studio sees the line "Read only here · Westlake keeps this record. Notes you write still save.", the record's fields locked, no Save bar and no Migration Hub. Before this, the long scroll offered them every editor and failed on Save. The form itself takes no edit from them either (`useRecordForm({ canEdit })`), so a control that ever escapes the lock cannot stage a change with no Save bar to save or discard it.
- **The watch-outs' injury rail says "none logged" only once the notes are read.** While they load it says nothing; when they failed it says "Injury and incident notes couldn't be loaded, so some may be missing." (the long scroll said "none logged" either way).
- **Coverage** is now `coverageOfClient` with the client's HOME studio's cutover (it was the active studio's, and ignored Mindbody's visit count), one value for Programming and the codex. The home is read as the rules read it (`recordStudioIdOf`: `homeStudioId`, else the older `studioId`). **The floor screens still use the iPad's studio** (the Active Session, the Clients list), so for a cross-train client Programming and the floor can word coverage differently until they move to the home too — a follow-up, not done here.
- **The progress-reports listener** moved into `useProgressReports` with its own render test (ready, failed, a client switch, out of quota) — phase 6's carried item. It now stays open while the record panel is mounted, so moving between Notes & Profile and the Activity Archive no longer re-subscribes.

**Typecheck baseline: 10 → 4.** Six of the ten pre-existing errors were in `ClientInfoSheet.tsx`, deleted here. The four left: `AppContent.tsx` ×2, `clinical-review/charts.tsx`, `EditTrainerModal.tsx`. No new error.

**Deviations from the plan, and why.**
- Experience moved to Body & Pulse in this phase (an additive `part` prop on `ActivityExperienceBaseline`), not in the FORD phase: `FIELD_HOME` puts the experience fields on Body & Pulse → Training story, and "Show" has to land on a real card from the first commit. The FORD phase still splits `LifeBaseline` into its three editors.
- `GoalsPanel` gained an optional `anchors` prop, so "Show" lands on "The why" and "Working toward" rather than the top of the page. Left out, it renders as before.
- Read only is interim: the record's editors sit in a disabled fieldset (`pages/RecordLock.tsx`) until each area's read views and Edit buttons replace them. Buttons inside a locked card (a contract's "show all", the goal history's "more") are disabled too for those readers.
- The shell spec's `stripEntries` / `noteZones` / kit `CriticalStrip` are not built: Notes' `CriticalLine` and `record-selectors` (phase 7) are the one version. The Story segment has no sub-line yet rather than a "since {year}" worked out without the whole story.
- `lastUpdatedBy` is written with `authTrainer.id`, as the old form did. Nothing reads it (pre-existing).

**Scale ratchet.** `CODEX_FILES` gains the shell's 17 files; `HOSTED_FILES` lists the moved sections and the pieces the pages host (19 files), whose off-scale text sizes are counted: **168**, the budget each area phase lowers. `BARE_PALETTE_BUDGET` 259 → 258 (the count).

**Tests.** `access.test.ts`, `record-form.test.ts`, `codex-data.test.ts`, `page-meta.test.ts`, `ClientCodex.render.test.tsx` (35 cases: pages and mounting, one listener each, none on FORD for a cross-trainer, no read on a switch, the critical line on five pages and its failed state, the Save bar's where / Show / Discard / Save / a refused save, a new snapshot of the record re-seeding only while nothing is unsaved (and a held form never writing back what it held), read only and a form that refuses edits for that reader, the watch-outs rail on a failed read, the Migration Hub door, nothing scrolled or focused while the tab is hidden, the neighbours and Done, a door scrolling to its card, no duplicate ids, no ResizeObserver or scrollIntoView), `useProgressReports.render.test.tsx`; `goals.render.test.tsx` now drives the panel with the real `useRecordForm`; the nav tests lose the shim. No Firestore, rules, index or Functions change.

**On an iPad (AJ):** at 744 and 834 portrait and in landscape, both themes — the bar wraps without clipping; an edit on FORD shows "1 unsaved change · FORD · Occupation" from Account, and Show goes back to it; sign in as a trainer from a cross-train studio and the pages are read only with no FORD; a saved wingspan shows on Body & Pulse → Build.

**Worth knowing.** `vite build` now warns that the `ClientProfileView` chunk is 511 kB (gzip 124 kB), over the 500 kB advisory; it was 496 kB before this phase, so the codex's +15 kB pushed it over. Advisory only; lazy-loading `ClientCodex` on the record tab is a cleanup-phase candidate. Pre-existing and unchanged: the clients update rule re-validates the merged document (`isValidClient`: first and last name non-empty and under 50 characters, `isActive` a boolean, `remainingSessions` a number), so an old record missing one of those refuses every Save with the plain "Couldn't save. Nothing was changed."

**Carried to phase 9.** The shell does not scroll to `note-{id}` or `notes-compose` (Notes' own anchors): the Overview's note rows, "Write a note" and the critical line's button open Notes at its top until the Notes page interprets them.

## Phase 9 — the Notes page

**Notes is built.** The Notes page is `src/features/client-notes/NotesPage.tsx`, inside the kit's Page (`client-codex/pages/NotesPage.tsx` is the adapter), on the tab's ONE load — it opens no listener of its own and has no fallback journal hook. `ClientJournalTab` loses its Notes area (it keeps Focus and Pulse until those pages are rebuilt). Read `src/features/client-notes/README.md` → "The Notes page". Top to bottom:

- **Banners** only when needed: the read guard rail, the missing index, and — new — **"Some of her notes couldn't be loaded, so what's below may be missing some."** A failed read never reads "No notes". No amber anywhere.
- **The composer is folded behind "Write a note…"** and drawn hidden when closed, so Close keeps the words ("Finish your note…"). The cursor goes into it only when it is opened, never on mount.
- **FORD / Life saves in place.** The same box and the same words become a FORD capture: no loudness or window, the four letters optional, "Save to FORD" → `clients/{id}/ford` only, stamped `fordStudioIdOf`. A FORD detail holds 2,000 characters (the rule) where a note holds 5,000, so a longer one says "FORD details hold up to 2,000 characters — this one is 2,001." and waits. A second tap on the chip is a note again, words intact. Offered to a reader who may change the record; a cross-train visitor is told where FORD is kept (the create rule would refuse them). The quick-note dialog gets the same mode, and stays open with "Saved to FORD".
- **A save that fails keeps the words** and says "Not saved — still here, try again". The composer used to keep them only by accident when a save failed; the page and the quick-note dialog now rethrow, and the composer catches.
- **The To-file tray** shows a loud note's Loudness pill: an unfiled critical note sits there, not in Open, and must still look critical.
- **One filter row**: search (every note, its updates and who wrote them — the coach chip row is gone), "All" and six category chips counting resolved notes too, always in the same places, and the **"Life · in FORD"** door, whose number shows only when it is known (none for a cross-train reader; none while loading).
- **Open** as full cards, loudest first, two columns from 720px wide (a 744pt portrait iPad gets one), a critical note across the whole width; **Standing context** one line each (the machine first and in full, the note's first line, who and when), opened in place; **Resolved** folded to "Resolved · N" and "Show the N resolved notes", then month by month, "closed Aug 7" / "ended Sep 20".
- **A critical note is drawn once**, in Open. "Critical & pinned" is gone; when a chip or a search hides a critical note, the critical line says so above the zones and "Show it" brings the card back.
- **The thread card is one panel**: kind (glyph and label, neutral ink), the machine, the Loudness pill — the only colour on the card (crimson Critical, plum Heads up) — the note, the spine of updates (open by default in Open), who and when (`threadCardMeta`), the **briefing line** (on your next briefing · hushed by you · from a date · off the briefing after three weeks), "Add an update" and a close whose words fit the note ("All healed up" only for an injury or incident; an equipment note simply closes). The ⋯ menu's **Archive takes the whole thread** — the root and every update in one batch — after an inline confirm; archiving the root alone used to leave its updates behind as notes of their own.
- **"No need to remind me" / "Show it again"** on the record, writing only `noteDismissals/{Auth uid}`, offered once the dismissals are read. The record never hides a note.
- **Doors land.** The critical line's "Open the note" and the Overview's rows open the thread (clearing a filter that hides it, opening its row, unfolding Resolved) and ring it for two seconds; **a critical note still in the To-file tray lands on its tray card** (phase 7's carried item); the Overview's "Write a note" opens the composer with the cursor in it; `notes-resolved` unfolds Resolved. Each is acted on once per move, and one made while the notes load waits for them (phase 8's carried item).
- **Settled life notes stay on Notes** in this phase; the FORD phase moves them into their pillars.

**Files.** New: `client-notes/NotesPage.tsx`, `ThreadRow.tsx`, `notes-intent.ts`, `notes-page.css` (+ `NotesPage.render.test.tsx`, `notes-intent.test.ts`). Rewritten: `NotesCatalog.tsx`, `NoteThreadCard.tsx`. Changed: `note-catalog.ts` (the filter is `{ category, search, showResolved }`; `NOTES_PAGE_CATEGORIES`; every zone holds every item and Resolved always has its months; `RESOLVED_PREVIEW` and `catalogCoaches` are gone), `threads.ts` (the Open and Standing lines), `record-selectors.ts` (`threadCardMeta`), `thread-write.ts` (`archiveThread`), `hooks/useClientJournal.ts` (`archiveJournalEntries`: one batch writing `isArchived` and `updatedAt` only), `components/journal/JournalComposer.tsx` (FORD in place, `onFordSaved`, keeps the words on a failed save, every small line 12px), `NoteSweep.tsx` (the pill), `QuickNoteDialog.tsx` (rethrows), `notes.css` (the old tiles, shelves and thread styles removed; `nc-hint`, the letters, the pill), `ford/ford-write.ts` (`FORD_BODY_MAX`), `client-profile/profile-nav.ts` (`threadIdOfAnchor`), `client-codex/ClientCodex.tsx` (hands Notes' anchors to the page), `components/journal/ClientJournalTab.tsx` (no Notes area), comments in `CriticalStrip.tsx` and `types/journal.ts` (on the record page a card's colour is its loudness only).

**No Firestore, rules, index or Functions change.** The writes are the ones the Notes area always made (a note, a FORD detail through `createFordEntry`, filing, close and reopen on the root, an update), plus `archiveJournalEntries` (`isArchived` + `updatedAt`, which the journalEntries update rule already allows) and the trainer's own dismissals document.

**Deviations from the plan, and why.**
- **"All" counts every thread the search finds**, not the six chips' sum: settled life notes are still on Notes in this phase (and a live life note stays there after the FORD phase), so a sum of six would count fewer notes than the zones show.
- **No window chip on the card's top row.** The mockup puts the window in the meta line ("Jess · Mar 15 · matters until Mar 22"); the spec had it in both. The meta carries it, and "ended Sep 20" once it ran out.
- **No `autoFocus` prop on the composer.** KNOWN-TRAPS forbids `autoFocus` inside a codex page (hidden pages mount too); the page focuses the box when it opens it.
- **The failed-read banner does not say "it retries on its own"** (the spec's copy): a failed listener is finished until the profile is opened again, so it says so.
- **The FORD line and the composer's FORD hint say "the home studio"**, not "people who can open her record": a cross-train trainer can open her record but not her FORD.
- **The FORD door's number is phase 7's `fordDoorCount`** (FORD's own count once read; the client document's counts while it loads; null when unknown or not this reader's) — no older-life-note count until the FORD page carries those notes.
- **The doors ride the shell's one navigation**, as INTEGRATION says: `ClientCodex` turns `note-{id}` / `notes-compose` / `notes-resolved` into a request keyed by the move (`notesIntentOf`), rather than separate `focusThreadId` / `composeOnOpen` props with handled-callbacks.
- **Composer and tray sizes.** They are shared with the session sheet and the post-session screen and keep their sizes there; on the page, `.nx-notes` rules bring each off-scale rule of `notes.css` onto the scale, and a new scale-test case fails if one is missing. The shared Loudness control keeps its 13 / 10px (one control everywhere) — the one exception on the page. `HOSTED_OFF_SCALE_BUDGET` stays 168: removing the Notes area moved no text size.

**Review fixes, before the commit.**
- **No chip counts when the notes could not be read**, as while they load: "All 0", "Injury 0" beside "couldn't be loaded" was the failed-read-is-empty mistake. The zone headers keep theirs — they count what is drawn under them, under the banner that says some may be missing.
- **A door to a composer already open puts the cursor back in it** (open on Notes, go to the Overview, tap "Write a note"): the focus effect keys on a tick that is new per open, not on the open flag.
- **A refused FORD save is tested** (the words stay, "Not saved — still here, try again", no journal write), and `saveToFord` catches a throw too, so no path clears the box or fails silently.
- **The quick-note dialog offers FORD in place only to a reader the create rule accepts** (`codexAccess(...).canEdit`, as the page); a cross-train trainer gets the hand-off there as well, where before they could type a detail and have it refused.
- **One "Show the N resolved notes", not two**: with nothing filtered, the empty line leaves it to the Resolved header's toggle.
- **A row's kind never breaks mid-word** in the 112px portrait column (`overflow-wrap: break-word` on its own span, not `anywhere`).
- **The composer's FORD hint says "the client's home studio"**, not the first name — the same words as its hand-off line.
- **Seen on the floor, and worth a look on the iPad:** the composer is shared with the Active Session notes sheet, so the sheet's small lines are now all 12px (they were 11, 11.5 and 12.5), and a refused save there now says "Not saved — still here, try again" beside the kept words. Nothing else in the sheet changes: FORD / Life still switches to "Remember this", and the draft still survives the sheet closing.

**Tests.** `NotesPage.render.test.tsx` (the zones, rows opening in place, Resolved and its months, chips counting resolved notes, the empty filter, the hidden critical note and "Show it", the door, no counts while loading, the four briefing lines and the hush writing only the trainer's dismissals, the tray's Critical pill and a door landing on it, doors to standing and resolved threads once per move, a door waiting for the notes, the composer and Resolved doors, a door refocusing a composer already open, one "Show the N resolved notes" when nothing is open, empty / migrating / failed (no chip counts) / large, FORD in place only for a writer), `NoteThreadCard.render.test.tsx` (close words, archive whole behind a confirm, the briefing line, no palette class), `notes.render.test.tsx` (now on `NotesPage`: FORD in place in the same box, the 2,000-character guard, the words kept when a note or a FORD detail is refused, and no Save to FORD in the quick-note dialog for a cross-train trainer), `note-catalog.test.ts`, `record-selectors.test.ts` (`threadCardMeta`), `notes-intent.test.ts`, `ClientCodex.render.test.tsx` (the critical line lands on the card; Write a note opens the composer; the door's number and FORD in place for the home studio; neither for a cross-train reader, whose note still saves), and the scale test's override case. Suite **271 files / 4,165 tests** (was 269 / 4,120). Typecheck **4** (the phase-8 baseline). Build passes (`ClientProfileView` 501 kB, down from 511). Case check clean.

**On an iPad (AJ):** at 744 and 834 portrait and in landscape, both themes — Notes opens with the bar, not the composer; a critical note shows once, full width; a Standing row opens in place and folds again; "Show the N resolved notes" works; picking Equipment on a client with a critical injury note shows the red line with "Show it"; typing, then FORD / Life, keeps the words and "Save to FORD" lands the detail on FORD; from Account, the critical line's "Open the note" rings the card on Notes; the Overview's "Write a note" opens the composer with the keyboard up; signed in as a cross-train trainer — no FORD number, no Save to FORD (on the page or in the header's Note dialog), and a note still saves. In an Active Session, open the notes sheet: the small lines read at one size and nothing else has moved. At 744 and 820 portrait, a Standing row's kind ("PREFERENCE", "COACHING TIP") wraps at a space, never mid-word.

## Phase 10 — the FORD page (without the new fields)

**FORD is built.** The FORD page is `src/features/ford/page/FordPage.tsx` (`client-codex/pages/FordPage.tsx` is the adapter), on the tab's ONE load — it has no hook of its own, so it cannot open a listener. It replaced the Life section's hub (`FordSection`, deleted) and the work and activity baselines above it. Read `src/features/ford/README.md` → "The FORD page". Top to bottom, as the mockup has it:

- **Coming up** leads with the **birthday Mindbody holds** — "Her 69th birthday · In 17 days · Family · from Mindbody" — rolled forward on the studio's day through the same `daysUntilBirthday` Relay's Mine uses, beside FORD's dated details (`coming-up.ts`). The when is coloured by urgency (`urgencyOf`: this week FORD's "now" orange, this month "soon" blue), never by pillar. An annual FORD "Birthday" on the same day folds into the birthday's row and its gesture rides on it; a legacy `client.events` birthday is dropped the same way but not linked. Tapping a birthday nobody planned for opens a new annual Family "Birthday" detail with the gesture open. Three cards, then "All N coming up".
- **To file**: each capture says where it was caught and by whom ("Caught mid-session by Jess Moreno, Mar 15"), and four 40px buttons carry the letter AND the word — no more letter-only buttons with a tooltip.
- **Four pillar cards**, every one built the same way: head (mark, name, FORD's own blurb, Edit / Done where the band is record fields, Add), the **band**, every standing fact, FORD's moments merged with the **older life notes** filed there (three, then Show all), the **Pulse lines**, a gap line, and **Ask next**. No count chip on the head.
  - **Occupation is the Work block and Recreation is Active outside the studio.** Edit opens the editor in the band; its fields go through the codex's one form, so Done only closes it, the band says "Not saved yet", and the Save bar says "FORD · Occupation". The job title is **free text with suggestions** (a `<datalist>` of the old list's 19 titles; `OccupationSelect`, with its raw hex and always-dark popover, is deleted) and Retired is two picks, Working and Retired. Family is the emergency contact and the birthday from the record; Dreams quotes the why from Goals & Focus in full, with a door there.
  - **Older life notes move here from Notes.** `notesOfJournal` passes `lifeOnFord`, so a settled journal `life` note (standing or resolved) leaves Notes' zones and counts and shows in its pillar, "From an older note · Jess Moreno · Sep 20, 2025" — Birthday and Anniversary under Family, Vacation under Recreation, the rest in an "Older life notes" card below the pillars. A live one stays on Notes. They are journal notes, so a cross-train reader who is refused FORD still sees them. A note with updates keeps them one tap away ("Show 2 updates"), because it can no longer be opened on Notes.
  - **Pulse lines** (`pulse-links.ts`, no Firestore, no writes) quote the Pulse beside the pillar it is about, in the Dial's own words — "Pulse stress, “Caring for someone”: Moderate on Dec 9, Mild on Mar 10." under Family, work and retirement under Occupation, "I stay physically active outside of workouts." under Recreation — with a footnote that nothing is copied between FORD and the Pulse.
  - **Ask next knows about retirement** (`ask-next.ts`, `FORD_PROMPT_WHEN`): a retired client is never asked "How is work treating you?" ("How is retirement going?" — "The work questions are skipped because she is retired"), a working one never how retirement is going. **The briefing cue passes the same context** — the one floor-visible change of this phase.
- **Going above and beyond**: Idea → Planned → Done with "I'll do it" (the signed-in person, by Auth uid) and "Mark done" ("What actually happened? Worth a sentence."), through `setGestureStatus`, the Delight queue's own writer. Owners' names in full.
- **The sub-toggle's FORD line is FORD's own** (`fordSubnavLine`): "birthday in 17 days", else "a date in 12 days", else "2 to file", else "6 details", else "nothing on file yet". The count — on the sub-toggle and on Notes' "Life · in FORD" door — is FORD's details **plus the older life notes** (`olderLifeCountOf`), null while the journal loads.

**The dialog.** `FordDetailDialog` **stays open with every word when a save is refused** ("Not saved — still here, try again"); the hub closed it and ignored the failed create, so the sentence was lost. **A new idea is unowned**: the dialog used to make whoever typed an idea its owner, so the Delight queue's "Needs an owner" was never true for one; now an idea has no owner until someone says "I'll do it", an existing owner is kept, and one moved to Planned or Done with nobody on it is owned by whoever moved it. The title is 17px and its icon buttons 40px. New props `initial`, `openGesture` and `bodyPlaceholder` serve the birthday and "Add an idea".

**The life editors.** `LifeBaseline.tsx` is now `WorkEditor`, `RecreationEditor` and `ExperienceEditor`, built on the kit's pick pills through `client-life/controls.tsx` (`PickGroup`, `ChipPicks` — the old `Seg` and `ChipSet`, same behaviour: tap the chosen pill to clear it, custom chips up to 40 characters, 24 at most, a 40px Add). `ExperienceEditor` is Body & Pulse → Training story in the interim Body host. `client-life.css` imports its tokens and is on the codex scale.

**Also.** `DelightQueue`'s empty line and the Operations → Delight queue intro say where a gesture is made now (a client's FORD page), not the Life section's gift button. `ford.css` loses the hub's rules (the briefing row, the queue banner, the dialog's buttons, the gesture chip and the notice stay). The kit's `TextInput` gains an optional `onBlur` (the job title trims on blur).

**Files.** New: `ford/ask-next.ts`, `coming-up.ts`, `pulse-links.ts`, `page-model.ts` (each with a `.test.ts`), `ford/page/` (`FordPage`, `PillarCard`, `bands`, `ComingUp`, `UnfiledTray`, `AskNextLine`, `AboveAndBeyond`, `ford-page.css`, `FordPage.render.test.tsx`), `client-life/controls.tsx`. Rewritten: `client-life/LifeBaseline.tsx`, `client-life.css`, `client-codex/pages/FordPage.tsx`. Changed: `ford/types.ts` (`FORD_PROMPT_WHEN`), `ui.tsx` (`pillarPrompt` takes the context), `FordDetailDialog.tsx`, `FordBriefingCue.tsx`, `DelightQueue.tsx`, `ford.css`, `client-life/life.ts` (`isRetiredClient`, `activitySentence`, `recreationSentence`, `OCCUPATION_SUGGESTIONS`), `client-codex/codex-data.ts` (`lifeOnFord`, `olderLifeCountOf`, `fordCountOf`), `ClientCodex.tsx` and `page-meta.ts` (FORD's line), `pages/NotesPage.tsx` and `record-selectors.ts` (the door counts the older notes; `olderLifeCount: null` is unknown), `pages/BodyPage.tsx` (`ExperienceEditor`), `kit/fields.tsx`, `admin/AdminDashboardView.tsx` (copy). Deleted: `ford/FordSection.tsx`, `components/OccupationSelect.tsx`.

**No Firestore, rules, index or Functions change.** The writes are FORD's existing writers (`createFordEntry`, `updateFordEntry`, `tagFordEntry`, `archiveFordEntry`, `setGestureStatus`) and the record form's one `updateDoc`. No rules test changed, so this phase needs no `test:rules` run.

**Deviations from the plan, and why.**
- **"In one line" and "Follow up next time" are not here** — phase 11, as INTEGRATION splits them. Ask next is always one of FORD's prompts for now; `ask-next.ts`'s `AskNext` has only its `prompt` kind.
- **One selector each, as INTEGRATION rules**: the older notes are Notes' `olderLifeNotesByPillar` (no FORD-side `olderLifeNotes`), the count is `fordDoorCount` with `olderLifeCount` (no `fordDetailCount`), "where" is `FIELD_HOME` (no `FORD_BAND_WHERE`), the read state is phase 1's `status` (no `readState` / `readError`), and the Pulse is the shell's one history (not `historyFromDocs(progressReports, length + 1)`, which would always claim completeness).
- **A failed FORD read still lets a trainer add** (phase 1's `fordCanAdd`: never block a save); the area spec disabled writes whenever the read was unknown. A refused read, or a client with no studio, offers no add.
- **The Pulse stress lines use the two newest rounds that said anything about stress**, not simply the two newest rounds: a Pulse is filled a piece at a time, and "not raised on Mar 10" about a round that never asked would read as an answer. A statement line uses the two newest rounds that answered it.
- **The kit's `FordMark`** (22 and 40) and **the kit's `inTime`** are used rather than a `letterSize` on `ui.tsx`'s mark and a `fromNowLabel` of FORD's own — one mark, one way of saying a date.
- **Single-choice picks are the kit's toggle pills (`aria-pressed`)**, not `role="radio"`, so the life editors behave like every other codex editor.
- **The sub-toggle line is lower case and plain** ("birthday in 17 days"), like every other segment's and the mockup's; a FORD date is "a date in 12 days" (a detail's `subject` is usually a person's name). `ProfileSubnav` has no meta colour, so the line is not coloured by urgency — it is never coloured by pillar either.
- **The bands' "Body & Pulse reads this line too" sources are left out** until Body & Pulse shows the line (phase 12) — a claim the screen cannot yet keep.
- **No intake slot on the Occupation and Recreation bands**: INTEGRATION puts the Mindbody intake offers on Account only (phase 17), so the same line cannot be added twice from two pages.
- **The hub's CSS goes now**, not in the cleanup phase: the component it styled is deleted here. `HOSTED_OFF_SCALE_BUDGET` falls from 168 to 148 (the FORD hub gone, the life editors on the codex list).
- **The "habits to unteach" switch stays a switch** (ExperienceEditor moved as it was); only Retired became picks.

**Review fixes, before the commit.**
- **The birthday card plans nothing until FORD has answered.** While FORD loads, or after the read failed, the client's own "Birthday" detail may exist unseen, and tapping the card would have opened a second one; until then an unplanned birthday card is read only (a linked one still opens).
- **The FORD count never turns a failed read into "none".** When the journal's notes could not be read and FORD holds nothing, the sub-toggle says "couldn't load" (not "nothing on file yet") and the Notes door shows no number (not "0"); FORD's own details are still counted when there are some. One helper, `fordCountOf` in `codex-data.ts`, feeds both.
- **A chip tapped on and off leaves nothing unsaved.** `ChipPicks` keeps the order the list was saved in (a new pick goes on the end) instead of the pills' order, so the Save bar no longer shows a phantom change for Recreation or Before Max Strength, and the recreation line reads as it was entered.
- **Tests for what had none:** the briefing cue for a retired and a working client with nothing under Occupation (mounted, not only the pure rotation), a refused FORD read for a trainer who may edit the record, and the birthday card while FORD loads or failed.
- **Two stale lines** said a gesture's outcome shows on "the client's Life section" (TESTING-CHECKLIST) and that `ford/` is "the Life section of the record" (`src/features/README.md`); both now name the FORD page.

**Tests.** `ask-next.test.ts` (the drift guard; 60 days of rotation for a retired and a working client; no context is today's rotation), `coming-up.test.ts` (17 days / 69th / soon; today; yesterday rolls to next year; bad dates; urgency thresholds; the linked and the legacy birthday; sorting; `ordinal`), `pulse-links.test.ts`, `page-model.test.ts`, `life.test.ts` (the four new helpers), `FordPage.render.test.tsx` (Coming up and planning a birthday, the tray's labelled buttons, Show all, an older Anniversary in Family, retirement-aware Ask next, the Pulse's own words, Edit → "Not saved yet" with no write, read-only readers, the four read states never saying "Nothing on file yet", the dialog keeping the words on a refused save, owners kept and ideas unowned, I'll do it and Mark done, every button 40px, no clipping), `LifeBody.render.test.tsx` (every old `updateField` assertion kept, now through `OccupationBand`, `ExperienceEditor` and `RecreationEditor`, plus the free-text title and its datalist), `ClientCodex.render.test.tsx` (the Save bar cases now Edit then pick Retired; an older Anniversary note is on FORD's Family card and off Notes, counted on the door and the sub-toggle, and still shown to a cross-train reader), `codex-data.test.ts`, `record-selectors.test.ts`, `page-meta.test.ts`, and `useClientFord.render.test.tsx` (the Life section's cases moved to the FORD page's test). Suite **276 files / 4,259 tests** after the review fixes (was 271 / 4,165). Typecheck **4** (the phase-8 baseline). Build passes (`ClientProfileView` 517 kB, up from 501 — the FORD page). Case check clean.

**On an iPad (AJ):** signed in as a Life Transformer, at 744 and 834 portrait and in landscape, both themes — FORD opens with Coming up (her birthday, "In N days", the when orange only this week); the pillars are one column in portrait below the 13-inch and two from about 880pt (the 13-inch portrait and landscape); nothing is cut off. On Occupation, Edit → type a job title that is not on the list, pick Retired → "Not saved yet", Ask next changes, the Save bar says "FORD · Occupation"; Save, and the band reads "Retired — was …". A client with an old "Anniversary" note in the journal: it is in Family, "From an older note", and gone from Notes. File a capture from To file. "I'll do it" on an idea puts your name on it. Remember something → save → "Saved to FORD." Signed in as a trainer from another studio: the notice that FORD is kept by the home studio, the older notes still there, no Add, no Edit. On the briefing, for a retired client with nothing on file under Occupation, the FORD line never asks about work.

## Phase 11 — FORD's new fields: In one line and Follow up next time (AJ's decision 3)

**In one line** (decision 3a). The sentence a new trainer should read first — "Retired hygienist, pickleball regular, walking the Camino with Tom in May." — now heads the FORD page (`ford/page/OneLinePanel.tsx`) and the Overview's FORD door, with "Written by the team · last by Jess Moreno, Mar 15" under it. It is the team's: anyone at her home studio may rewrite it, it saves the moment Save is tapped (it is FORD, not a record field, so the Save bar has nothing to do with it), last writer wins, and an empty box clears it. Up to 120 characters, one line.

- **Where it lives: `clients/{id}/ford/one-line`**, as INTEGRATION ruled — a FORD document with a fixed id, not a field on the client document (a cross-train studio reads that; FORD it cannot) and not a collection of its own (a rules change, a deploy and one more listener, for nothing). It is stamped with `fordStudioIdOf(client)` like every detail, so the ONE FORD listener already delivers it; `useClientFord` takes it out first (`splitOneLine`, before the legacy merge) and hands it out as `oneLine`, never among `entries` / `buckets` / `untagged` / `upcoming`.
- **Stored `isArchived: true`, `pillar: null`, `kind: "one-line"`** on purpose, so every reader of details skips it — the tray, the rollup, Coming up, the Delight queue, the sweep, and an old iPad bundle mid-deploy. `one-line.test.ts` proves it never reaches the tray, the rollup or Coming up.
- **Writes** (`saveFordOneLine`): the first line is the whole document (`setDoc`, the Auth uid as author); every rewrite after is an update of the words and who wrote them — never `studioId` or `clientId`, which the rule holds immutable. A cleared line is rewritten by the same `setDoc`, which the rules take as an update. It does not refresh the rollup.
- **What it says, by state.** Ready: the line, or "No line yet. The one sentence a new trainer should read first." with "Write the line". Loading: "Loading…". Failed: "The line couldn't be read just now, so this isn't the same as no line." — **and no editor**: a line is replaced, not added to, so it is rewritten only over one this iPad has read (a detail is still offered on a failed read, as in phase 10). Refused (a cross-train visitor): not drawn at all — the page's notice already says whose FORD it is. A reader who may not write FORD sees it read only.

**Follow up next time** (decision 3b). `followUp`, `followUpAt`, `followUpBy` on a FORD detail — a question for the next trainer, at most 140 characters.

- **Written only by the detail dialog** ("Follow up next time (optional)", under the date), and **stamped only when the question changes** (`followUpPatch`): the dialog sends every field on every save, and re-dating an unchanged question would reorder Ask next. A new detail stamps who and when if it has one; three nulls otherwise, never `undefined`.
- **Read by Ask next.** The pillar's Ask next is the newest open follow-up on its details — "“How did the new boots do?” · Follow up from Jess Moreno, Mar 15" — ahead of FORD's own prompts (including the retirement one). An archived, resolved or legacy detail has none.
- **"Asked it"** opens: "What was the answer?" (optional), **Save the answer** (a new detail under the pillar, same subject, the trainer's words — then the question is cleared), **Nothing new, clear it** (`clearFollowUp`: all three to null, nothing else touched) and **Not yet**. A refused save keeps the words and says so; if the answer saved but the clear did not, it says THAT and does not offer to save the answer again, which would file it twice.
- **No question waits unseen.** A detail holding a follow-up Ask next is not showing (only the newest shows) says so after it with the question itself — "follow up next time: “Still Tuesdays?”" — a standing fact included; a capture still in To file shows its question there.

**No Firestore rules, index or Functions change.** Both fields ride the ford block as it is. Rules tests added (against the unchanged rules), "FORD — In one line and Follow up next time": a home trainer creates the line as themselves and not as anyone else; not stamped with another studio, and not empty; a second home trainer rewrites it and clears it, but cannot move its studio or client; the create again over a cleared line (the fallback); the app's one query returns it with the details; a cross-train visitor is refused it to read and to write although they read the client record; **its last author may delete it and nobody else may** (INTEGRATION's correction of the area spec — no screen offers it); a home trainer adds and clears a follow-up, a visitor may not; a new detail may carry one from the start. Local emulator run (demo project): **154 / 154**. AJ's `npm run test:rules` is the one that counts.

**Files.** New: `ford/one-line.ts` (+ `one-line.test.ts`), `ford/page/OneLinePanel.tsx`, `ford/ford-write.test.ts`, `ford/page/AskNextLine.render.test.tsx`. Changed: `ford/types.ts` (`kind`, `followUp`, `followUpAt`, `followUpBy`; `FordDraft.followUp`; `shortDate`, moved from `page-model.ts`), `AboveAndBeyond.tsx` (its import), `ford.test.ts` (`shortDate`), `ask-next.ts` (the follow-up kind, `openFollowUps`, `hasOpenFollowUp`, `normaliseFollowUp`, `followUpPatch`, `FOLLOW_UP_MAX`; `askNextMeta` takes `now`), `ford-write.ts` (follow-ups in create and update, `clearFollowUp`, `saveFordOneLine`), `useClientFord.ts` (`oneLine`), `FordDetailDialog.tsx` (the field), `page/FordPage.tsx`, `AskNextLine.tsx` (Asked it), `PillarCard.tsx` (the marker), `UnfiledTray.tsx`, `ford-page.css`, `page-model.ts` (`fordOverview().oneLine`), `client-codex/pages/OverviewPage.tsx` and `codex.css` (the door's line), the scale test's `CODEX_FILES`, `tests/firestore.rules.test.ts`, the FORD README, KNOWN-TRAPS, TESTING-CHECKLIST.

**Deviations from the plan, and why.**
- **The interim Overview carries the line on its FORD door** (the full FORD slot is phase 18): the line and "Written by the team · last by {name}, {date}", only when FORD answered for a reader the rule accepts; otherwise the door is as it was. `fordOverview` returns `oneLine` for phase 18 to draw.
- **One read shape, as INTEGRATION rules**: `useClientFord` gains `oneLine` only — no `readState` / `readError` (phase 1's `status` is the one shape).
- **`askNext(pillar, { retired, seed, entries })`**, the phase-10 signature with `entries` added, rather than a positional `entries`; `askNextMeta` gains `now` so a follow-up set last year says its year.
- **The line panel is not drawn for a refused reader** (the spec disabled its input) — the line is FORD text, and the notice already says whose it is.
- **Gaps the spec left, filled**: a capture's follow-up shows in the tray, a standing fact carries the marker as a moment does, and "answer saved, question not cleared" has its own sentence.
- **The FORD page has an In one line panel the mockup does not draw** (the mockup shows the line on the Overview only). It is where the line is written, as the area spec has it.

**Review fixes, before the commit.**
- **A client who moved home studio is told the truth.** Her old line sits at the fixed id stamped with the old studio: the new studio sees "No line yet", and every save there is a `setDoc` the rules refuse as a `studioId` change — forever, which is worse than an older detail (only hidden; new ones still save). `saveFordOneLine` now answers `saved` · `failed` · `blocked`, and a refused FIRST line (`blocked`) says a line from an earlier home studio may be in the way, that trying again won't help, and that an administrator can clear it (the delete rule allows administrators and franchise owners; no screen offers it). "Try again" is said only when trying again can work.
- **An open "Asked it" holds its question.** Real Firestore applies `updateDoc` to the cache before the server answers, so the listener showed the question cleared at once, Ask next moved on, and the line — keyed per question — remounted: the panel closed as if the clear had worked, "The answer is saved…" never showed, and a retry would have acted on the next detail. `AskNextLine` now keeps the question it was opened on (and its meta) until it closes, showing the live words while that detail is still the live one; every action is handed the question it is for; the page gives the line no per-question key. `AskNextLine.render.test.tsx` hands the line a moved-on Ask next mid-clear, and `FordPage.render.test.tsx` holds the update open (`holdUpdate`) — the old fake applied nothing locally first, so it could not see this.
- **Save stays reachable in the detail dialog.** The new field made it about 90px taller, and it had `overflow-hidden`, no height limit and `max-w-lg` without `sm:max-w-lg` (384px on an iPad — the width trap in `components/ui/dialog.tsx`). It is now 512px, at most the screen's height less 2rem, and scrolls. TESTING-CHECKLIST gains the 11-inch landscape check with the keyboard up.
- **A waiting question is readable by everyone.** The marker carries the question ("follow up next time: “Still Tuesdays?”"), so a reader who may read FORD but cannot open the dialog sees it too.
- **Quotes typed around a question are taken off** (`normaliseFollowUp`: one pair of straight or curly double quotes around the whole of it, none inside), so Ask next no longer shows “"How were the boots?"” — the dialog's example is in quotes, so trainers type them.
- **In one line's field is labelled "The sentence"**, not "In one line" under a card that already says so; **"Saved" shows for 2.5 seconds** and goes at once if another trainer's line arrives.
- **Tidier.** `clearFollowUp` is a plain update with no rollup refresh (a follow-up is not in the rollup; "Save the answer" refreshed it twice). The "Mar 15 / Mar 15, 2025" date is one `shortDate` in `types.ts`, used by the page, Ask next and In one line (it was written three times).

**Not done here, for AJ.** The briefing does not read follow-ups yet — "next time" really happens in the briefing, and showing the newest open follow-up there is a floor change that needs your yes (the-floor.md). `fordSummary.pinned` is untouched (HELD). A client who has moved home studio cannot get a line at the new one until an administrator clears the old line — the panel says so (open question 11); a screen that lets an administrator clear it, or moving the line with the client, is yours to decide.

**Tests.** `one-line.test.ts`, `ford-write.test.ts` (the stamps, `clearFollowUp`, the line's create / rewrite / clear / refusals, no rollup refresh), `ask-next.test.ts` (follow-ups ahead of prompts, the pillar, what is ignored, the order, the meta; `followUpPatch` stamps only on change and clears all three), `useClientFord.render.test.tsx` (the line arrives on the one listener and never among the details; cleared or unread is null; its words never hide a legacy event), `page-model.test.ts` (`fordOverview().oneLine` only once ready), `FordPage.render.test.tsx` (the line's first write, rewrite, clear, refusal, Cancel, the four states, the refused reader, read only; Ask next's follow-up, Asked it's three moves and both failures, the marker, the tray, the dialog's stamp only on change), `ClientCodex.render.test.tsx` (the line on the Overview door and the FORD page from the one listener, never a detail, never for a cross-train reader). After the review: `AskNextLine.render.test.tsx` (an open panel keeps its question while Ask next moves on, says the answer is saved when the clear is then refused and retries THAT question; live words for the same detail; a closed line follows Ask next), `FordPage.render.test.tsx` (the held question against a held update, the blocked first line never saying "try again", the field's one label, "Saved" for a moment and not under another trainer's line, the question readable by a reader who cannot open the dialog), `ford-write.test.ts` (`blocked`, and no rollup refresh on a clear), `ask-next.test.ts` (the quotes), `ford.test.ts` (`shortDate`). Suite **279 files / 4,332 tests** after the review fixes (was 276 / 4,259). Typecheck **4** (the phase-8 baseline). Build passes (`ClientProfileView` 527 kB, up from 517 — the line panel and Asked it; Vite's 500 kB note is advisory). Case check clean. Rules tests re-run after the fixes (unchanged file): **154 / 154** on the local emulator.

**On an iPad (AJ):** signed in as a Life Transformer, portrait and landscape, both themes — FORD → Write the line → Save; the Overview's FORD door leads with it. A second trainer at the same studio edits it and their name replaces yours. In a Recreation detail, type a Follow up next time and Save; Ask next shows it with your name and today; edit only the sentence and the date under the question stays; Asked it → Save the answer adds a detail and the question goes. A cross-train trainer sees no line anywhere. On an 11-inch in landscape, open the birthday plan from Coming up and tap into Follow up next time: with the keyboard up the dialog scrolls and Save is reachable.

## Phase 12 — Body & Pulse: build, where it matters, watch-outs, floor, measured and told, Pulse

**Body & Pulse is built.** The page is `src/features/client-codex/body/BodyPulsePage.tsx` (`pages/BodyPage.tsx` is the adapter). It reads first and edits on demand, top to bottom as the mockup has it — read `src/features/client-codex/README.md` → Body & Pulse:

- **How to coach her** — the first paragraph of the coach strategy on her record, verbatim, with a door to Goals & Focus ("All of it" when there is more). Interim: Goals & Focus's `howToCoachLead` (phase 14) replaces the sentence.
- **Build** — where she sits against the height the catalog's machines are set for ("Shorter than our machines are set for." — women 5'4", men 5'9", no gender 5'6½", 3" either side), her reach ("1" less than her height"), weight (a scan's over a typed one, each with its source), body fat, age · sex ("From Mindbody"), her work and what she does outside (FORD's fields, "Edit work on FORD"), then the **Training story** moved from Life (decision 6). A typed height the app cannot read says so and how to write it; Edit shows the three boxes with a live "Reads as 5'4"." line. The saved wingspan shows.
- **Where it matters** — the body figure (front and back): a plum **diamond** on the midline for every region a watch-out names (a flag records no side), an ink **ring** on the side she named in the Pulse pain map. Every clinical flag has a place (`FLAG_REGIONS`; the test fails on a new flag without one); arm flags (elbow, wrist and hand) have no midline and are listed, not drawn; whole-body flags are listed under "Whole body"; an id the clinical list does not have is listed as such. The region list is the answer — each region a 44px button that opens its sentences ("She told us: right knee, Mild (Pulse, Mar 10), Moderate on Sep 16.", "Brought on by Leg Press.") and lights its marks. Then her injury notes (injury and incident threads), listed with their loudness, each opening on Notes; one the critical line already carries is not repeated.
- **Watch-outs** — every instruction her flags carry, **quoted** from the clinical matrix, one group per instruction listing every floor machine it names (a 40px button each, to the machine window), "Every set" groups first, then Stop · High · modify. A flag's bracketed detail is printed in the heading (it was a hover tooltip). Then the medical history and the constraints, quoted. Edit opens the flag picker and the two text boxes. It replaced the long scroll's banner (`BodyWatchOuts`, deleted), which only listed machine names.
- **On our floor** — per prescribed machine (Routine A in order, then B when B is on, then any other machine carrying a note of hers): HER notes first (her open and standing threads on the machine, loudest first, then the machine notes marked important), then the Academy's column for her band, quoted part by part. Average build or no height: one line says the standard set-up applies, and the prescribed machines with nothing to add are counted, not drawn. **Clients built like her** is machine fit's own sentences with its minimum of 5 — no invented ranges, no value to use — and a door to Setup.
- **Measured, and what she told us** — Strength (InBody skeletal muscle, first against latest, "A 1.2 lb change is inside the scanner's normal variation (±3.5 lb, Max Strength's default), so it isn't called a change." — or, with no scan, the renewal check's "Stronger on 6 of 9 machines…") beside "I feel stronger than I did 3 months ago." with its word; a row for each region with both a flag and a told spot; Consistency (the renewal pace) beside "I am consistent with my workouts." Each side keeps its own source and date.
- **Pulse · the living assessment** — one line per area in the three pillars (one statement per area, the most recently answered, "“I wake up feeling rested.” Often · Mar 10"; the pain map; stress worries in her own words with "also shown on FORD · Family"; days a week for protein and hydration; "over 90 days, due a look"). **Update Pulse** swaps in the existing Pulse panel over the page's ONE draft; **Hand to client** opens its client mode.
- **InBody** — last, on the kit now (`showTrends={false}`: the timeline, phase 13, will draw the trends).

**The "Recovery between sessions" select is gone from the screen** (decision 7); `recoveryMetric` stays on old records, is marked `@deprecated`, and the record form refuses it.

**Reads.** The journal, the Pulse history and the InBody scans are the tab's one load. The page's own reads happen once, when it is first opened: the client's ONE Pulse draft (`useCheckInDraft` — the panel gets it through `draft`, never a second copy), the machine catalog (the collection the app already streams, for the Academy's body-type columns) and machine fit's reads for "clients built like her" — the same cached studio index and company trend reads Programming → Setup makes, and **none for a client with no height**. Her settings, routines, the studio roster and the studio come from the profile (`ClientCodex`'s new `programming` prop → `CodexData.programming`). No sessions query (the arrive/leave track, phase 13, reads the journal's 40 sessions).

**Also.**
- The sub-toggle's Body & Pulse line is the page's own (`bodySubline`): "2 watch-outs" with the plum dot (crimson only for an absolute contraindication), else "Pulse Mar 10", else "Build and Pulse" (it said "none on file").
- `ClientJournalTab` loses its Pulse area (Focus is all it mounts now). `machine-fit/ui/stored-spec.ts` holds the "Similar to" spec, moved out of `SetupView` unchanged, so Setup and Body match clients the same way. `clinical-flags.css` loses the banner and its sizes collapse onto the codex scale (the flag picker's names are 14px now, its badges 11px — seven sizes from 9.5 to 14 before). The InBody card is on the kit and tokens (it was Tailwind emerald / amber / sky / slate): its scan rows wrap and show who entered each at every width.
- `HOSTED_OFF_SCALE_BUDGET` 148 → 125.

**No Firestore, rules, index or Functions change.** The writes are the record form's one `updateDoc` (Build, Training story, Watch-outs), the Pulse's own autosave through the one draft, and the InBody card's existing batch. No rules test changed, so this phase needs no `test:rules` run.

**Deviations from the plan, and why.**
- **The how-to-coach strip quotes the first paragraph**, not the whole coach strategy — the strip is a lead line, and the whole is one tap away on Goals; it is interim until Goals' `howToCoachLead` (which is also first-paragraph).
- **`useCheckInDraft` is called by `BodyPulsePage`**, not `PulseCard` (INTEGRATION's shared-ownership line), because the figure and the Measured-and-told pairs read the same draft as the Pulse card.
- **One selector each, as INTEGRATION rules:** her notes per machine are Notes' `threadsByMachine`, her injury notes Notes' `injuryThreads(…, { includeIncidents: true })` (no `injuryThreadsOf`), and the Pulse's saved rounds are the tab's one history (the page's draft is the editor and the open round).
- **Machine fit reads nothing for a client with no height** (the spec gated on the page only): there is nothing to compare her with, and the card says to add a height.
- **Arrive/leave is phase 13.** `figure-map.regionRows` already takes the door's taps (`door`) and has its sentence and test; nothing passes it yet. The timeline, `arrivals.ts`, `timeline.ts` and the Over-time card are phase 13's.
- **"Clients built like her" shows machine fit's verdict without its count** ("Nothing looks unusual for this build.") because the card's head already says "14 of 16 machines set up".
- **The Academy text is the catalog's**, as the machine window reads it (a studio's override of `bodyTypeAdjustments` is not shown on either — the open question to change both together stands).
- **Headings use the pronoun** ("How to coach her", "Clients built like him", "Measured, and what they told us") from Mindbody's gender (the overnight default).
- **The Outside row keeps the trainer's own spelling** ("Pickleball, Walking" — `recreationSentence`, which never lower-cases), rather than the spec's lower-case join.

**After the review.** A read that has not answered is unknown on every card, not only on the Pulse card:
- **Measured, and what she told us** says "Loading what she told us…" while the saved rounds or the open round are still out, and "Not known: the saved Pulse couldn't be read" after a failed read. It says "Not asked yet" only once the Pulse answered (`toldMissingLine`, pairs.ts). **Where it matters** draws the open round's pain map over a failed history and then says only what is missing ("The saved Pulse couldn't be read just now; only the open round is drawn.").
- **On our floor** holds back the "N more of her machines use the standard set-up" count, "Not in her routines", "No machines in her routines yet", the "N of M machines set up" head and the Clients-built-like-her verdict until her routines, her settings and the machine catalog have all answered (`floorReads`, floor.ts), and says which read is still loading or failed. `CodexProgramming` gains a `status`, which `ClientProfileView` works out from its routines read and its settings listener. That view now also clears the settings when the client changes (as it already cleared the routines), and ignores a routines read that lands after the client changed. `useMachineCatalog` gains an additive `failed` flag.
- **Copy.** With no coach strategy the strip says "No coach strategy on her record yet." — it checked only the strategy; Goals' `howToCoachLead` adds the coaching and preference notes in phase 14. The Weight hint says any InBody scan's weight is shown ahead of the typed one (it said "a newer" one, which the code never checked). "The critical notes" is plural when the line carries more than one.
- **A region's sentences sit beside its button** (`aria-controls`), not inside it, so a screen reader names the button by its region and its line only.

**Tests.** `build.test.ts`, `figure-map.test.ts` (every matrix flag placed, every region with a spot; diamond on the midline, ring on the named side, both sides two rings, resolved none, back view mirrored; row sentences and order; whole body; the door), `watchout-groups.test.ts` (one group per instruction with every floor machine; every-set first; Stop · High · modify; the set-up change; nothing reworded), `floor.test.ts` (her critical note first, then an important machine note, then the Academy's words verbatim; resolved notes out; average band none; B only when on; routine order), `pulse-read.test.ts` (draft over report; the living rule; words not numbers; the shown statement and ties; the last different answer; pain reversed and matched on region and side; the read grid's lines; stale), `pairs.test.ts`, `built-like-her.test.ts`, `page-lines.test.ts`, `BodyPulsePage.render.test.tsx` (no Recovery select; the saved wingspan; no Edit for a cross-train reader; no Tailwind size or colour; the every-set quote and a machine chip opening the machine; the diamond at 60,203 and the ring at 49; a region row opening; the Heads-up injury note listed and the critical one not; failed notes and a failed Pulse never read as none; Update Pulse over the one draft with no second progressReports read, kept mounted after Done; Hand to client opening client mode; the Strength and knee pairs), `InBodyCard.render.test.tsx` (the tone as `data-tone`; `showTrends={false}`; every scan with who entered it and nothing cut; Add scan on the kit's button), `ClientCodex.render.test.tsx` (Build opens its editor before the wingspan box; the injury notes under the figure; Body's new anchors present once), `page-meta.test.ts`, `LifeBody.render.test.tsx` (the banner's cases moved to the page's test). After the review: `pairs.test.ts` (the told side while the history loads or failed, never "Not asked yet"; the open round's answer over a failed history), `floor.test.ts` (`floorReads`: each read loading or failed, and the band that needs no Academy column), `BodyPulsePage.render.test.tsx` (Measured and told on a failed and a loading Pulse; the open round's pain map over a failed history; a region's sentences beside its button; the floor on a failed, a loading and a known programme). Suite **288 files / 4,433 tests** (was 279 / 4,332 after phase 11). Typecheck **4** (the phase-8 baseline). Build passes (`ClientProfileView` 576 kB, up from 527 — the page; Vite's 500 kB note is advisory). Case check clean.

**On an iPad (AJ):** signed in as a Life Transformer, at 744 and 834 portrait and in landscape, both themes — Body & Pulse opens with how to coach her, then Build and Where it matters side by side with Watch-outs across under them (three across in landscape). For a client with a knee flag and a knee in her Pulse: the diamond sits between the knees, the ring on her side; tap "Knee" and the sentences open. On Watch-outs, tap a machine chip: its window opens. Edit Build, type a height the app cannot read: the line under the box says so; Save. Update Pulse, answer one area, Done: the grid shows the new answer with today's date. Hand to client: the client's sheet opens. A client with no height: Build says so, On our floor says the Academy's set-ups can't be matched, and "Clients built like her" asks for a height. A cross-train trainer: no Edit anywhere, the Pulse still updates. Nothing is cut off at any width.

## Phase 13 — Body & Pulse: Over time, how she arrives and leaves (AJ's decision 9)

**Over time is built** — the card between Measured, and what she told us and the Pulse (`src/features/client-codex/body/OverTimeCard.tsx`, drawn by `BodyTimeline.tsx` from `timeline.ts`, with `arrivals.ts` reading the sessions). Read `src/features/client-codex/README.md` → Body & Pulse.

- **It leads with the everyday half** (decision 7): the briefing's "How's the body since last time?" over her recent sessions, in words — "“How's the body since last time?” asked at 12 of her last 24 sessions. “Still feeling it” or “Still wrecked” at 3 of them."
- **One six-month timeline** (26 weeks, on studio day keys), lane by lane in this order: the door — recovery always, then sleep, energy and stress when they were asked at least once — then how the session landed (the dose, judged by the trainer); then the Pulse — the statement the read grid shows for Sleep & Recovery, Strength & Physical Confidence and Pain & Mobility, and at most two pain-map spots, most severe first; then InBody — skeletal muscle and body fat % with the shaded band of the scanner's normal variation around her FIRST scan (her HOME studio's numbers, decision 8), then weight with no band.
- **Each reading sits where she gave it, on its own words**: a bar per session on the Dial's five words (below the centre in ink, the rest muted), rings and a dashed line for the Pulse (better is higher; pain has None at the top), dots and a solid line for InBody. Never a score, a percentage or a red.
- **The footer** says how each reading is drawn, the counts it knows — Journey's, not her whole six months ("1 saved Pulse round, 2 InBody scans and 24 sessions in Journey in these six months."), the coverage caveat when Journey may not hold her whole story, and "The 412 sessions she had before Journey (FileMaker) aren't drawn." when she has a prior record.
- **The figure's region list** now says what was tapped at the door: "At the door: “Stiff” on Mar 20 · tapped at 3 of her last 12 sessions." (phase 12 built the sentence; this phase hands it the taps).
- The page's lede is the mockup's again: "…side by side and over time."

**No new read.** The sessions are the journal's own: `useClientJournal` already streams her 40 newest session documents (no exercise logs) and hands them out as `recentSessions` — the spec's `useRecentSessions` hook and its gated `getDocs` were not built (INTEGRATION's ruling). `SESSION_SUMMARY_LIMIT` is exported from the hook so the page can tell a full page from all of her sessions. The shell's render test holds it: one `sessions` listener however many pages are visited, and no sessions `getDocs`.

**Unknown is never "not asked".** An untouched dial is null and counts as not asked — no mark is drawn for it — and a line needs `RULE_OF_THREE` answers ("not enough for a line yet (needs 3)"). A client with no sessions reads "Not asked yet: Journey holds no session of hers in these six months." — never "As usual". Only a session the briefing ran for is ever called unasked (below, after the review). While the journal's sessions load, the door and the dose are one "Loading her recent sessions…" row (the lede meanwhile says only what the card is about, so the state is said once); a failed listener reads "Her recent sessions couldn't be loaded just now, so how she arrived isn't drawn. The rest of this page is unaffected." with no Try again (the listener's error is final), and the Pulse and InBody still draw. A loading or failed Pulse or InBody read is its own row, and the footer leaves out a count it does not know.

**Her last N sessions, never all.** The listener orders by the `date` string, so a full page of 40 may stop short of six months; `pageReach` works out since when the count holds from the page's smallest date string ("her last 40 sessions (since Mar 3)"). A page crowded with old "9/1/2024"-style dates — which sort above every yyyy-mm-dd day — draws nothing rather than guess which sessions are recent. The Pulse lanes likewise say "since …" when the saved rounds the tab holds don't reach back six months.

**No Firestore, rules, index or Functions change.** Nothing is written; no `test:rules` run is needed.

**Deviations from the plan, and why.**
- **One strip per lane, not one SVG.** Each lane's title and sentence are HTML over its own SVG strip, every strip on the same x for the same day, with the month axis under the last lane. A single SVG cannot wrap text, and at 744–834pt portrait a long Pulse statement or sentence would have been cut off at the drawing's edge (the codex never clips). Each strip is `role="img"` with a `<title>` and a `<desc>` that says the whole lane in words.
- **The lede for a question never asked** reads "The briefing asks “How's the body since last time?” at the door. It wasn't asked at any of her last 12 sessions." — the spec's "Asked at the door in the briefing. Not asked at any…" read as a contradiction. With no sessions at all it says "Not asked yet: Journey holds no session of hers in these six months."
- **The dose never says "at or above Just right"**: its right side is "too little", not "better", so with no "Drained" or "Wiped out" it says "Never “Drained” or “Wiped out”."
- **A lane's lines don't repeat its title**: the question is the lane's title, so its line says "Asked at 5 of her last 24 sessions. …" (`arriveDetail`); the full sentence is its `<desc>`. The recovery lane says only where it is asked — its sentence is the card's lede.
- **InBody with no scan in the window is one row** ("Last scan Aug 1, 2026, before these six months."), not three empty lanes; with none at all, "No InBody scan in Journey yet."
- **The band's words are in the lane's line**, not drawn inside the plot (they would clip at portrait widths): "InBody · the shaded band is the scanner's normal variation around her first scan (Oct 2): ±3.5 lb, Max Strength's default."
- **"One session" is said plainly** ("asked at her one session in these six months") rather than "1 of her last 1 sessions", in the lane, the lede and the figure's door sentence.

**Tests.** `arrivals.test.ts` (legacy `sleepQuality: "poor"` → −1 and `clientFeel: "Wiped Out"` → −2 through session-reads; an untouched dial null and not asked; in-progress and undatable sessions dropped; M/D/YYYY parsed; the window; the body map onto the figure's regions, every briefing region mapped; `pageReach` all · since · below-2000 · unknown; loading and failed are never empty; every sentence branch at 0, 1, 2, 3 and 12 answers, the since clause, the dose's wording, the lane detail; no "next time", progression or percentage anywhere; the door taps), `timeline.test.ts` (the window is 182 days on day keys across the clock change; the month ticks; lane order — the door first, InBody last; a question never asked has no lane and draws no mark; Dial positions and below-centre; a statement on the frequency words and a pain spot with None on top; one round is not a line; out-of-window answers dropped; the open round's answer on its day; at most two pain spots, most severe first; the band on her first scan ± her home studio's numbers, set-by wording, % on points, never weight; one scan, an old scan, no scan, a failed read; sessions loading and failed — no "Not asked", no "Try again", no count; Pulse loading, failed and truncated; the lede and the footer; no score), `BodyTimeline.render.test.tsx` (mounts with no ResizeObserver at 640; follows one and disconnects it; lanes in order, each strip's `<desc>`; the band on muscle and not weight; no mark where not asked; no alert token in the markup or the timeline's CSS; the loading mark and the failed sentence; a point's word kept off the gutter at the window's start; the axis), `BodyPulsePage.render.test.tsx` (the lede from the journal's sessions with no sessions read or listener of the page's own; not asked — never as usual — with no sessions; the failed and loading sessions reads; the Knee row's door sentence), `ClientCodex.render.test.tsx` (no sessions `getDocs` however many pages are visited; the Body page draws the journal's sessions; `body-timeline` is an anchor in the page once), `figure-map.test.ts` (the one-session door sentence). Suite **291 files / 4,512 tests** after the review below (4,497 before it; 288 / 4,433 after phase 12). Typecheck **4** (the phase-8 baseline). Build passes (`ClientProfileView` 600 kB, up from 576 — the card; Vite's 500 kB note is advisory). Case check clean.

**After the review.** Two majors and three wording fixes, before the commit:
- **"Not asked" only where it could have been asked.** The first build counted every completed session in the window, so imported sessions (FileMaker, a chart), sessions from Log past session, and — for recovery, which has no legacy field — every session before the question was added on Sep 16 all read as "not asked". A client with thirty old-briefing sessions opened with "It wasn't asked at any of her last 30 sessions", and a migrating client with three imported chart sessions with "…at any of her last 3 sessions" (the migration rule: an empty Journey history is "no detail here"). Now `originOf` (run in Journey · imported · logged later — a session with no start time at all is imported: the chart importer writes none even when it matched the trainer) and `couldAsk` (plus `RECOVERY_ASKED_FROM` = 2026-09-16 in `rating/scales.ts`) decide the count, and what is left out is named: "“How's the body since last time?” asked at 2 of her 3 sessions run in Journey since the question was added (Sep 16), not enough for a line yet (needs 3). 1 more was imported, and imports don't record the door." — or, when none could record it, "The briefing asks “…” at the door. Her 30 sessions in these six months came before the question was added (Sep 16)." A session that did answer always counts. The figure's door count uses the same rule ("tapped at 2 of her last 5 sessions run in Journey").
- **The Pulse row no longer claims "No Pulse answer saved in these six months"** when a saved round in the window answered only areas the timeline doesn't draw (the card's own footer counted that round). It now reads "1 saved Pulse round in these six months, with no answer to the statements drawn here; the Pulse card below has every area." ("since …" on a truncated history); the old sentence is kept only when no saved round falls in the window and the history read reaches its first day, with a clause for an open round that answered other areas.
- The single-scan Weight line reads "InBody · one scan in these six months; two or more draw a line." (it had lost its separator); the loading and failed sessions state is said once (the lede is "How she arrives at the door and how each session lands, over these six months." until the sessions are known); the footer's counts say "in Journey in these six months".
- Tests: `arrivals.test.ts` (`originOf` for a live session, a Timestamp start, FileMaker, "Chart", "legacy-trainer", a chart import with a matched trainer and Log past session; imported and logged sessions never "wasn't asked" or "wasn't judged"; the left-out sentence beside a count; thirty pre-Sep-16 old-briefing sessions never "wasn't asked" while sleep still counts all thirty; counting from the day the question was added; an early answer still counted; every reason when none could record it; the door count's `runOnly`), `timeline.test.ts` (a nutrition-only round and a truncated history never read "No Pulse answer saved"; the history that reaches the window's start and an open round on another area; the Weight line's exact words; a migrating client's lede; the lede never repeating the gap row), `figure-map.test.ts` ("run in Journey" in the door sentence), `BodyPulsePage.render.test.tsx` (the failed and loading sentences appear once; a migrating client's imported sessions), and the session fixtures now carry the start time every session run in Journey has.

**On an iPad (AJ):** signed in as a Life Transformer, at 744 and 834 portrait and in landscape, both themes — open a client who has trained in Journey for a few weeks: Over time leads with the recovery sentence, the recovery lane has a bar at each session where it was asked (none where it wasn't), the lane titles and sentences wrap and nothing is cut off at either edge, and the months line up under every lane. A brand-new client with no sessions: the lede says "Not asked yet: Journey holds no session of hers in these six months." and nowhere says "As usual". A client whose sessions were imported from FileMaker or a chart: the lede says they were imported and don't record the door — never "wasn't asked". A long-standing client: the recovery sentence counts from Sep 16 ("since the question was added"). A client with two InBody scans: the shaded band sits around her first scan on Skeletal muscle and Body fat %, not on Weight. Tap Knee on Where it matters for a client whose knee was tapped at the briefing: "At the door: … tapped at …". Turn the iPad: the timeline redraws at the new width.

## Phase 14 — Goals & Focus

**Goals & Focus is built.** The page is `src/features/goals/GoalsPage.tsx` (`client-codex/pages/GoalsPage.tsx` is the adapter). It reads first and edits on demand, three rows as the mockup has them — one column under 760px of page width (the 744pt iPad held upright), two from 760 (820 and 834 upright, the 13-inch, landscape) — read `src/features/goals/README.md`:

- **How to coach her** — the coach strategy on her record, quoted with its own line breaks ("Coach strategy, on her record"), then **From her notes**: her Preference and Coaching-tip threads exactly as Notes lists them (Notes' `howToCoachThreads`, by KIND — a legacy session wrap-up is never a "Preference", a focus check-in stays on its focus), loudest first, labelled in Notes' words, Heads up and Critical shown, the machine named first; a long note folds to two lines and a tap opens the thread on Notes. Six, then "All n in Notes". Edit opens the strategy.
- **Her why** — the why on her record as a lede in curly quotes (quote marks already typed round it are taken off first), then the other places it is written, verbatim and read only: Mindbody's long-term goal, the consultation (`client.goals`, which now has a reader here), the sign-up notes' Goals line (the intake matcher fills it in phase 17 — Account stays the one place a Mindbody line is ever copied), and her Dreams in FORD with an Open door — only once FORD answered for a reader FORD lets in.
- **Working toward now** — the goal as a lede, five S M A R T squares (filled when ticked; not buttons), "All five ticked" / "3 of 5 ticked" / "Nothing ticked yet", the target ("May 1, 2027 · in 6 weeks") and Mark achieved. Edit opens the goal, the five toggles and the date. Every write is GoalsPanel's, unchanged: Mark achieved is four field edits the Save bar saves together.
- **Coach focuses** — the focus board on the kit ("Coach focuses · 2 running", Set a focus, each coach's card with its P, machine, "Yours" / "Review due", who set it and when, check-ins, and a "review Mar 23" still ahead), its history folded behind a 44px summary. Its writes are `useFocusActions` (the Auth uid; a check-in carries its focus id).
- **Plans from the team**, with **your working notes · only you** inside the same panel (the jot strip), and **Reached** — every goal marked achieved and every focus achieved, newest first, a goal marked a moment ago flagged "Not saved yet", and "Only what was marked in Journey…" for a client whose story began before Journey.

**How to coach her has ONE lead line** (`howToCoachLead`, `goals/goals-page.ts`): the first paragraph of the coach strategy, verbatim; with none, her first coaching note that is not Critical (the red line under the bar and the Overview's Notes slot carry those — a Critical note leads only when it is the only kind she has, marked Critical), whole, with who wrote it, its machine named first and its Loudness; "loading" or "couldn't be loaded" — never "nothing written" — while her notes are unknown. Body & Pulse's strip now reads it (its interim `coachStripLine` is gone), and the Overview's Goals slot will (phase 18, with `goalsGlance`).

**Unknown is never empty, on every card.** Notes loading or failed: How to coach says so. Focuses loading or failed: the Coach focuses card says so and offers nothing — the old journal area drew "No active focus" and Set a focus while they loaded — and Reached counts the goals only. FORD loading, failed or refused: "Reading FORD…", "Couldn't read FORD just now.", "FORD is kept by the home studio." — never "Nothing in Dreams yet". Nothing reads `fordSummary.pinned` (held for AJ; still written as before).

**The jot strip's duplicate-note bug is fixed.** Its read of the trainer's own notes settled as "none" when it FAILED, so the next jot started a second "working notes" note beside the one it could not see. The read now has three states, keyed by trainer and client; only a read that answered enables the box and Add, and a failed one says "Couldn't load your working notes, so a jot can't be added here now. Reopen the profile to try again." — a failed listener does not come back, so the words never promise that the notes will load (`ClientJotStrip.render.test.tsx`; KNOWN-TRAPS → Relay).

**Also.**
- The sub-toggle's Goals & Focus line is the page's own (`goalsTabHint`): "2 focuses running", else "a goal set", else "no focus running" (it said "nothing set").
- `GoalsPanel.tsx` is deleted (its only callers were the adapter and its test). The focus board left `ClientJournalTab`, which nothing mounts any more (the cleanup phase deletes it). `SharedNotesCard` and `ClientJotStrip` — mounted only here — are drawn with the kit and the codex's tokens (the Tailwind sky / slate / amber palette and the jot rules' hex fallbacks are gone; the jot strip's Open is 40px), and word the client with the pronoun. Mindbody's long-term goal moved to Her why; the other Mindbody indexes wait on the interim Account page.
- `HOSTED_OFF_SCALE_BUDGET` 125 → 93. `CODEX_FILES` gains the page, its cards, `goals.css`, `goals-page.ts`, `goals.ts`, `focus.ts`, `useFocusActions.ts`, `FocusBoard.tsx`, `SharedNotesCard.tsx` and `ClientJotStrip.tsx`.

**No Firestore, rules, index or Functions change.** The writes are the record form's one `updateDoc` (the strategy, the why, the goal and its checklist, target and history), the focus writes and check-ins as before, the shared plans' Take off the record as before, and a jot on the trainer's own note. No rules test changed, so this phase needs no `test:rules` run.

**Deviations from the plan, and why.**
- **One vocabulary for the rows.** A row's label is Notes' `noteCardLabel` ("Pace", "Coaching tip", "Preference", "Note"), not the spec's "Coaching tip · Pace", and its words fold to two lines (`.gf-row__text`) rather than one line with an ellipsis — INTEGRATION's rulings.
- **The lead note is the list's first row that is not Critical** (loudest first, then the one that moved most recently), not strictly the newest, so the strip, the Overview and the card agree and a Heads up tip leads. A Critical tip is passed over because the red line under the bar already carries it — drawing it again in the strip was the "one critical note drawn up to four times" the mockup's audit names; it leads only when every coaching note she has is Critical, marked Critical.
- **No "Use as her why" on Her why** (INTEGRATION: the intake matcher's actions live on Account alone). `signUpGoals` is a prop nothing feeds until phase 17 builds the parser.
- **Dreams is the newest standing Dreams detail, else the newest Dreams moment** — the spec took pinned details only, which would say "Nothing in Dreams yet" for a client whose Dreams are all moments.
- **The SMART sentence is the mockup's** ("All five ticked"), not the spec's "A SMART goal: all five ticked"; the squares' screen-reader name lists the ticked letters.
- **The focus board waits for the focuses** (new): until they are read, the card says so and offers no Set a focus.
- **"Jot a note" is left off the plans' head when the jot strip is inside the panel** (the mockup shows Write a plan only; the strip does the same thing in place). It stays wherever the card is mounted without the strip. The spec left this as a question.
- **GoalsPanel is deleted now**, not kept as a wrapper: the dossier that needed the wrapper went in phase 8.
- **Reached's caveat names no source** ("Anything reached before Journey isn't recorded here.") — the spec's "is in FileMaker" is not true of every prior record.
- **Goal dates are the studio's day** ("Jan 22, 2027" for 03:30 UTC on the 23rd), and a focus's span is `formatSpan` ("4 weeks").

**Fixed after review.**
- **Her words are never reworded by the quote-stripper.** `withoutOuterQuotes` took off any opening and closing mark, so `"Lose weight" and "get strong"` came back as `Lose weight" and "get strong`, and `'Tis the season, I'm told'` lost its apostrophe. It now takes off only a real pair: the same family (straight or curly, a double is a double), with nothing of that family between them.
- **The How-to-coach lead carries its machine and its Loudness, and passes over a Critical note.** A Leg Press tip used to show on Body & Pulse as a general cue, and a Critical tip was drawn twice there (the red line, then the strip). The strip now names the machine first, shows the Heads up or Critical chip, and leads with the first non-Critical note.
- **The Overview's glance (phase 18) never says "0 goals reached"** — for a migrating client, the goals may have been reached before Journey — and a focus with no start date reads "AJ", never "AJ, —".
- **The jot strip's failure words no longer promise a retry.** A failed listener does not come back while the profile is open, so it says to reopen the profile.
- **Plans from the team has render coverage with a plan in it** (`SharedNotesCard.render.test.tsx`): the kind dot, Read all and its links, a leader's Take off the record → confirm → one `removeSharedNote`, a refusal said in the confirm, nothing to change for a trainer who is neither author nor leader, Edit in Relay for the author.
- The Save bar still names the why "Goals & Focus · The why" (and "· How to coach"): its labels are fixed words shared by every client, so they carry no pronoun (goals README).

**Tests.** `goals-page.test.ts` (the lead: strategy, more, the note fallback with its author, machine and Loudness, a Critical note passed over unless it is the only kind, unknown while loading or failed, none; the rows: Notes' order, labels and Loudness, check-ins, closed tips, wrap-ups and equipment left out, six shown of all; Her why: both index spellings, quotes taken off once and only as a pair (two quotes, a leading apostrophe, mixed families left as typed), Dreams pinned then newest, loading / failed / home-only never "none", `fordSummary.pinned` never read; the SMART words and label; the target line; Reached: goals and achieved focuses newest first on the studio's day, "Not saved yet", goals only while focuses are unknown, the heading; the tab line; the Overview glance, with no "0 goals reached" and no "AJ, —"; no lookbehind), `goals.render.test.tsx` (every GoalsPanel assertion now on Her why, Working toward now and Reached through the real record form — the toggles and a toggle back, a date ticks T, Mark achieved writes exactly four fields with no `undefined`, no checklist written for a client without one — plus the read views, Edit, "Unsaved", "Not saved yet" and a reader who may not edit; the focus board's three cases unchanged, plus the folded history with its coach filter, and the review date), `GoalsPage.render.test.tsx` (14: every anchor once, the neighbours and Next to Story, one listener each for the plans and the jots and none of the tab's; How to coach's rows, order, Loudness, door, failed and loading, an edit left unsaved; Her why's lines, the Dreams door, a cross-train reader and a failed FORD; no Edit or Mark achieved for a reader who may not edit, Set a focus still offered; the focus card's head and folded history; nothing offered while the focuses load; the jot strip inside the plans' panel, the pronoun not the name; Reached's order, meta and caveat; no Tailwind size or colour), `ClientJotStrip.render.test.tsx` (a failed read disables the box and Add and saves nothing, even by keyboard; a pending read offers nothing; the three newest jots with Open; a jot files on the note that is there; a new note only when the read answered none; the stylesheet's 40px Open and sizes), `useFocusActions.render.test.tsx` (the board files through the hook as the Goals page wires it), `BodyPulsePage.render.test.tsx` (the strip: the strategy's first paragraph and All of it, a coaching note with its author, a Critical tip passed over and a machine named first with its Heads up chip, an only-Critical tip leading marked Critical, loading never "nothing written"), `SharedNotesCard.render.test.tsx` (new, 6: a plan in the list — its kind dot, Read all and its links, a leader's Take off the record → confirm → one `removeSharedNote`, a refusal said in the confirm, nothing to change for a trainer who is neither author nor leader, Edit in Relay and "You" for the author, no Tailwind size or colour), `ClientCodex.render.test.tsx` (Goals' own reads open on its first visit only, once; a cross-train reader's Goals page; `goals-reached` is an anchor in the page once), `page-meta.test.ts`, `page-lines.test.ts` (the strip helpers went with the strip). Suite **295 files / 4,571 tests** (291 / 4,512 after phase 13). Typecheck **4** (the phase-8 baseline). Build passes (`ClientProfileView` 604 kB, up from 600). Case check clean. The page was also rendered to static HTML with the real stylesheets and looked at in headless Edge at 744 (one column), 820 and 1024 (two columns), light and dark: nothing cut off.

**On an iPad (AJ):** signed in as a Life Transformer, at 744 and 834 portrait and in landscape, both themes — Goals & Focus opens with How to coach her (the strategy, then her Preference and Coaching-tip notes) beside Her why from 834, stacked at 744. Tap a coaching row: Notes opens on that thread. Edit the goal, tick all five, set a date: T ticks itself; Done, then Save on the bar. Mark achieved with a reward: the goal moves to Reached as "Not saved yet" until Save. Set a focus, check in on it, open Focus history. Jot a working note: it lands under Your working notes. As a cross-train trainer: no Edit and no Mark achieved anywhere, Her why says "FORD is kept by the home studio.", and Set a focus still works. Body & Pulse's How to coach her strip reads the same first paragraph as Goals; for a client with no strategy and a machine tip, the strip names the machine first. Open My Studio first, then a client with a shared plan on Goals & Focus: the plan's text should read as plain paragraphs — a known, older clash (NoteBody's `nb` class is also the Now Bar's in `relay/board/relay.css`) can give it the Now Bar's grid and border once relay.css has loaded.

## Phase 15 — Story

**The Story is built** — a new page; the long scroll had none. `src/features/client-story/`: `story.ts` is the pure half (`buildStory`, the filters, the fold, the since line, the sub-toggle's line), `StoryPage.tsx` draws it, and `client-codex/pages/StoryPage.tsx` is the thin adapter. Read `src/features/client-story/README.md`.

- **Her time with Max Strength, newest first, every line from a record somebody already made** — so nothing is typed twice, and the page writes nothing. The moments: a package started, renewed, come back after a gap, cancelled (when Mindbody stamped it) or ended with nothing after it; Mindbody's first visit ("First visit, as Mindbody records it." — or, when a schedule pull inferred it, "The earliest Mindbody visit Journey has seen; the first may be earlier."), with who referred her; the years before Journey as ONE dashed panel ("Mar 2019 – Sep 2026 · 412 sessions in FileMaker", where their detail lives, that she is never treated as new); the first session in Journey; goals and focuses reached; each dated step in protocol mastery; each InBody scan's printed numbers; each saved Pulse round, with the statements whose word moved quoted in full ("“I feel stronger than I did 3 months ago.” **Often**, was Sometimes."); critical and injury notes opened and closed; FORD moments and gestures marked done.
- **The page:** the since line ("With Max Strength since Mar 2019. 412 sessions in FileMaker before Journey, and 49 in Journey."), the filters (Everything · Milestones · Body & Pulse · Life · Goals & focus — the panel for the years before Journey is a milestone), what could not be read, then the years — the two newest open, an older one a 40px "8 moments in 2019" button; the year is a label, not a sticky header. A moment with a door is ONE 44px button that opens the card it came from (a note opens its thread on Notes); a long note folds behind "Read all". Neutral throughout: no crimson, no pillar hue, no hero orange; a milestone's dot is filled, the rest are outlines. Portrait first: the year sits beside its moments from 640px of page width (every iPad), above them below that; in landscape the moments keep a 1000px reading measure.
- **The sub-toggle's Story line** is the year of the page's own since: "since 2019", "since 2025 or earlier", "in Journey since 2026" (`storyTabHint`), so the Overview's Story door carries it too.

**No reads, and unknown is never empty.** The story is built ONCE in the shell (`useCodexData` → `CodexData.story`, over the tab's one load) and shared with the Overview's Story slot (phase 18). `storySourcesOf` (`codex-data.ts`) hands it each read as ready, pending, failed or `off` (FORD for a cross-train reader, which the tab never opens); a source that is not ready makes NO moment, and the page says so — "Still reading notes, so those moments aren't shown yet.", "Couldn't read InBody scans here, so those moments aren't shown.", "FORD is kept by the home studio, so FORD moments aren't shown here." "Nothing recorded yet." only when nothing is unknown. The shell's render test holds it: visiting Story opens no listener and makes no `getDocs`.

**Prior history is real history.** A migrated client is never "new" and never "First session." (only when Journey holds her whole story and there is no prior record; otherwise "First session recorded in Journey."). "First Pulse in Journey" only when the Pulse read reached her first report; the oldest round of a read that did not is only "Pulse saved." (its baseline is unknown), and "no earlier answer changed" is said only when every answer could be compared. InBody moments state the printout and never a change — decision 8's normal variation is Body & Pulse's call. Nothing dated after today is a moment yet.

**No Firestore, rules, index or Functions change.** Nothing is written; no `test:rules` run is needed.

**Deviations from the plan, and why.**
- **Since when is the Story's own rule, not `resolveClientSince`** (INTEGRATION said the date comes from it). That helper ranks Journey's first session FIRST, so a long-standing client with no prior record whose first Journey session was this month reads "Client since Sep 2026" in the header — a confident wrong date. `storySince` takes the earliest day the record can stand behind (the prior record's first day, Mindbody's own first visit, and Journey's first session only when Journey holds her whole story), then Mindbody's record, then "since at least" (an inferred first visit, the earliest contract, the prior record's last day), then "In Journey since". Where the header is right the two agree; where it is not, the Story says less, never more. **The header is not changed** (it has other readers: the progress report, Operations' moments) — flagged for AJ.
- **The since line states a month, not "7 years with us"**: the spec's year count was arithmetic on a date that may itself be a lower bound. The counts are the header's own (`sessionTotals`), as INTEGRATION ruled.
- **Sources are `Src<T>` with four states** (`ready`, `loading`, `failed`, `off`), not three: FORD at a cross-train studio is neither loading nor failed, and the page says whose it is. The journal is two sources (notes and focuses), because `loadState` answers for each on its own.
- **The first package reads "Started a package: …"**, not "Started …": for a client whose story began before Journey, the first package Journey holds is not her start.
- **A focus reached names its span in the source line** ("Focus · AJ · after 4 weeks"), not in the sentence, so the coach's intent is quoted as written (no full stop taken off to fit a clause). Its door is Goals & Focus → Reached (where achieved focuses are listed), not the focus board's folded history.
- **A Pulse round with an unknown baseline says "Pulse saved."**, with who in the source line ("Pulse · AJ"), rather than "Pulse saved by AJ." — one place for who, on every moment.
- **A long note folds at whole sentences when the first of them are long enough to read on their own** (120 characters), else at a space with "…" — the kit's rule against half a sentence, where it can hold.
- **The two newest years in her story start open**, not "this year and last": a client whose last moments are from 2025 would otherwise open on a page of folded buttons. Which years are open is decided on the whole story, so a filter never folds the year being read.
- **"Account → On file" is a door to an anchor the Account page adds in phase 16** (`account-on-file`, already in the registry); until then Mindbody's moments open Account at its top.

**Tests.** `story.test.ts` (35: a migrated client — every moment in order with its day, the FileMaker panel on the day Journey took over, a package started then renewed with no end while one follows, the first visit with the referral, a goal on the studio's day, a focus with its span and reward and no retired one, protocol mastery with no undated step, scans with no change words, Pulse quotes verbatim in words, critical and injury notes opened and closed, FORD moments and gestures but no standing fact, archived or unfiled detail, import or one line, no profile-field adapters, never "new"; a brand-new client's "First session." and first Pulse; unknown and partial coverage; an inferred first visit; pending, failed and refused sources; an incomplete Pulse read; came back, ended, cancelled and a future start; the filters; the fold; since when, including a long-standing client whose first Journey session is not her start; nothing invented for an undatable record; no lookbehind), `StoryPage.render.test.tsx` (11: the since line, the pronoun, neighbours and Next; years newest first with an older one folded until tapped; the panel under Everything and Milestones, not Life; an empty filter's Show everything; Read all; a door as one button with Read all outside it; the Pulse quote; what couldn't be read; "Nothing recorded yet." only when nothing is unknown; no crimson, hero or Tailwind; the door's row height, and no sticky year), `codex-data.test.ts` (`storySourcesOf`: every state of every source, `off` for FORD refused), `page-meta.test.ts` (the Story's line), `ClientCodex.render.test.tsx` (the Story from the tab's one load with its line on the bar and no read of its own; a moment's door landing on Goals & Focus → Reached; a cross-train reader told FORD is the home studio's, with no FORD listener). `CODEX_FILES` gains the page, `story.css` and `story.ts`. Suite **297 files / 4,643 tests** after the review fixes below (4,623 before them; 295 / 4,571 after phase 14). Typecheck **4** (the phase-8 baseline). Build passes (`ClientProfileView` 623 kB, up from 604 — the page and the story; Vite's 500 kB note is advisory). Case check clean. The page was also rendered to static HTML with the real stylesheets and looked at in headless Edge at 600, 744 and 1366, light and dark: nothing cut off, the year beside its moments from 640px and above them below.

**After the review.** Three fixes before the commit, and the cheap minor ones:
- **The since line said "before Journey" twice** for a prior record whose source is Other ("412 sessions recorded before Journey before Journey"). The line now has its own where-words (`PRIOR_SINCE_WHERE`), each saying it once: "in FileMaker before Journey", "on paper records before Journey", "before Journey (a trainer's estimate)", "before Journey". A case for every source.
- **The FileMaker panel vanished when its last day was after today.** The profile's "Counted up to" is a plain date field with no maximum, so a studio typing its planned cutover lost the page's one statement that those years happened. The panel now sits at today in that case (its words keep the month as typed), and `storySince` — given the studio's `today`, which the sub-toggle now passes — never says "since at least" a month that hasn't come.
- **An empty filter said "yet" while its read was missing.** A cross-train reader's Life filter always read "Nothing under “Life” yet." under "FORD is kept by the home studio". `unreadUnder` (with `STORY_READ_KIND`, each read filed under the kind of moment it gives) now makes it "Nothing under “Life” that could be read here."; an empty story with only FORD off reads "Nothing recorded yet that can be read here."; a capped journal leaves notes and focuses uncertain under their filters. Render cases for Life with FORD off and failed.
- **An inferred first visit is named for what it was taken from** (`firstVisitOf` now returns its basis). A `backfill:` from Journey's sessions or from a contract is no longer called "the earliest Mindbody visit": "The earliest session Journey has on file; …" / "The earliest package Journey has on file began; …", and a contract start is read as its UTC day.
- **One contract rule for both passes:** a term signed within the gap carries her on even if it was cancelled later, so the term before it is "Renewed" into and no longer also "ended".
- **The prior-record panel's edge cases:** wholly imported says "All of them have since been brought into Journey." without "can't show what happened in each one"; a record of 0 sessions makes no panel and dates nothing (the coverage rule already reads it as complete).
- **The unknown-coverage caveat appears once:** the since line keeps `COVERAGE_CAVEAT.unknown` (it is the Overview's line too); the undated panel says it in its own words.
- Not changed: `storySince` over `resolveClientSince` stays as it is, held for AJ above.

**On an iPad (AJ):** signed in as a Life Transformer, at 744 and 834 portrait and in landscape, both themes — open a long-standing client with a FileMaker record: Story leads with "With Max Strength since …" and the header's numbers, the FileMaker years are one dashed panel where Journey took over, and nothing says "new" or "First session."; the sub-toggle's Story line says "since {year}". Tap a Pulse moment: Body & Pulse opens at the Pulse; tap a critical note: Notes opens on that thread. Filter to Life: only FORD moments, no panel. Open a brand-new client: "First session." and "First Pulse in Journey". As a cross-train trainer: "FORD is kept by the home studio, so FORD moments aren't shown here." Compare the header's "Client since" with the Story's line for a client with no FileMaker record who has trained here for years — if they differ, the Story's is Mindbody's first visit (see the deviation above).

## Phase 16 — Account

**Account is built** — the last page of Notes & Profile. `src/features/client-admin/`: `AccountPage.tsx` is the page, `ContactCard.tsx` the ID card, `MembershipSection.tsx` the membership (it was `ContractPanel.tsx`, restyled onto the kit with the same logic and writes — the panel is deleted), `account.ts` the pure half; `client-codex/pages/AccountPage.tsx` is the thin adapter. Read `src/features/client-admin/README.md`. Read first, as the mockup has it — one column under 760px of page width (the 744pt iPad held upright), two from 760:

- **Contact** — ONE read view instead of a form of greyed-out boxes: goes by, legal name, born ("Apr 2, 1958 · 68", from the date's digits), gender, phone, email, address, emergency ("Tom Brennan · husband · (440) 555-0187") and the Mindbody ID — shown here and nowhere else on the page. An empty fact says why: "Not in Mindbody" once Master Sync has read her, "Not synced yet" before, "Not recorded" on a client Mindbody does not hold. Mindbody owns who a client is, so on a linked client the one edit is the nickname ("Set a nickname" / "Change" opens a box in place); a client Mindbody does not hold gets an Edit that opens her name, birthday, gender, phone, email, address and emergency contact (an emptied name says the record needs one — the rules refuse it). The card says "From Mindbody · synced 2 days ago · Sync is at the top of the profile" and has **no Sync button** (the mockup departure AJ's list accepts: Master Sync is the one sync).
- **Mindbody account notes** — as synced, verbatim with their line breaks, read only ("No account notes in Mindbody." / "Not synced yet." when there are none). Phase 17 turns this card into the intake matcher's, in the same place.
- **Membership — what she has bought, what is left, and where she can train.** **The package**: the package's name as the 30px line ("Committed"), its terms ("12 months · paying every 4 weeks") and where that came from, said once ("Read from the Mindbody contract “96 Sessions · 12 Mo Autopay”", "Locked by AJ"); what is left ("95 left · sessions left", "12 payments to go", "Renews Mar 14, 2028"), the pace, the renewal's sentence only when it says more than the columns ("Auto-renews … with about 6 sessions still banked", "Package ended … — no new one in Mindbody yet"), and "Time for the renewal conversation" in brand blue; Lock the tier, its seven choices (each naming its package — "18 mo · paid in full / Life Transformed"), Use Mindbody's. **Contract history** as tiles, oldest first, the years before Journey first and dashed ("Mar 2019 – Mar 2026 · 412 sessions in FileMaker · Before Journey"; with no prior record and a story Journey does not hold whole, "Before Journey · Not recorded here" with the coverage caveat). **Where she can train** (home, "Cleared to train at Westlake" / "NOT yet cleared…", the other studios as picks). **On file with Mindbody** (the waiver in its three states, the status — with "a prospect in Mindbody" and "marked inactive in Mindbody", which Master Sync wrote and nothing read until now — since when, with "first visit" only for Mindbody's own date, and Mindbody's own visit count). **How she found us** (lead source and referral, out of the fine print, with Edit). **The fine print**, folded behind a 44px summary: the contract's number, signing, seller and autopay, sessions on hand, active memberships, Mindbody's other indexes, the last commercial pull, when Journey last read her, and the Migration Hub.
- **The sub-toggle's Account line** is the page's own (`accountTabHint`): "package ended", Mindbody's own "95 sessions left", else "7 on hand", else no line.

**Every edit is the one form's**, and the Save bar saves: "Account · Contact" (the nickname, an unlinked client's identity), "· Membership" (the lock — the same object the contract panel built, `setById` = the Auth uid), "· Where they can train" (the cross-train studios — a home of their own now, `account-train-at`, so Show lands on the card), "· How they found us". Done only closes an editor. `renewal` is never written.

**Read only is each page's own now.** A reader who may not change the record (a cross-train studio) sees every fact — the client document, fine print included, is readable to them — and no Set a nickname, Edit, Lock, pick or Migration Hub. Account was the last page still hosting the long scroll's forms, so the shell's interim disabled fieldset (`pages/RecordLock.tsx`, `.cx-lock`) and `pages/legacy-blocks.tsx` are deleted with it; the form's own `canEdit` stays the backstop.

**Colour.** A package's tone is the card's left edge (`tabTone`): green on track, plum a caution — an ended or lapsed package included, never crimson. The contract panel's hero-orange callout is brand blue; `client-admin.css` is on the codex scale and tokens (its 10–20px sizes and its 900px viewport query are gone; the rows are container queries).

**No Firestore, rules, index or Functions change.** The writes are the record form's one `updateDoc` of the changed fields; no rules test changed, so this phase needs no `test:rules` run.

**Overnight defaults used:** pronouns from the gender Mindbody holds (she/he, they when unknown) in the page's words ("Her contact details…", "How she found us", "Where she can train"); no Sync button on Account (one of the four accepted departures). `fordSummary.pinned` is neither read nor written here (held for AJ).

**Deviations from the plan, and why.**
- **The Mindbody notes card is read only in this phase.** The plan's phase 16 has no intake matcher (phase 17 builds `intake.ts` and `IntakeNotesCard`), so the card shows the notes verbatim where that card will go, and only for a client Mindbody holds (or one an earlier link left notes on).
- **`legacy-blocks.tsx` and `RecordLock.tsx` are deleted now**, not in the cleanup phase: Account emptied the first and was the last user of the second. `DossierPrimitives` and `JournalRail` (read by nothing since this phase) stay for the cleanup phase, which names them.
- **An on-hand count is never "left".** With no renewal snapshot the contract panel printed Mindbody's on-hand sessions as "sessions left"; for a monthly client that is the few sessions between payments. The page says "7 on hand · sessions on hand in Mindbody", and the sub-toggle says "7 on hand" (the spec's `accountTabHint` summed them as "sessions left").
- **No "contact and membership" on the sub-toggle when nothing is known** (the spec's fallback): a blurb that long wraps the portrait bar (phase 4's measure), and the shell's line already printed nothing then. An ESTIMATE of what is left stays off the line too, as the shell phase decided.
- **The tier's source line is its evidence, else a word** — never both (the panel printed "From the studio's package table · Matched to … in the studio's package table"). The moved test's "Read from Mindbody names" became the contract it was read from.
- **The renewal chip's line appears only when it adds something**; on track, the two columns already say "95 left" and "Renews …" — and for a package paid in full, or banked sessions after billing ended, the date column says what the chip did: "Runs out around Sep 1 (estimated)" (the renewal's `runOutDate`, always an estimate), "Run-out date not known yet" with no pace.
- **The cross-train studios have their own Save-bar home** ("Where they can train", `account-train-at`) rather than "Membership".
- **The fine print is readable by a cross-train reader** (the spec's recommendation; it always was, behind the disabled fieldset); only the Migration Hub needs `canEdit`. The shell's render test now asserts that instead of the fine print being absent.
- **An unlinked client's On file card says once** "Not linked to Mindbody, so Mindbody has nothing on file for him yet." rather than "Not synced yet" four times over a Sync that cannot help; its fine print says "Last read: Not linked to Mindbody".
- **No `client-admin/access.ts`** (INTEGRATION: `codexAccess` is the one answer); `canEdit` comes from the shell.
- **`accountGlance`** (the Overview's Account slot) is built and tested now, for phase 18; nothing reads it yet. `ageAndBirthday` counts the birthday with the Hub's `daysUntilBirthday`, so the two cannot disagree.

**Also.** `HOSTED_OFF_SCALE_BUDGET` 93 → 80 (`ContractPanel.tsx`, `client-admin.css` and `legacy-blocks.tsx` left the hosted list; `client-admin/*` joined `CODEX_FILES`). The palette ratchet (`neutral-ramp.test.ts`) 258 → 242, the count again. KNOWN-TRAPS → The client profile gains the Account bullet (a linked client's identity is read off the RECORD, never the form, which may hold a name older than Master Sync's; on hand is never "left"). The Goals README's pointer to "the interim Account page" now points at the fine print.

**Tests.** `account.test.ts` (28: the ID card's order and values, the date of birth from its digits on and before the birthday, why an empty fact is empty in all three cases, an unlinked client's typed identity and which facts are editable, a linked client's record never overridden by a stale form, the nickname in the pronoun, a temporary profile; `ageAndBirthday`; On file — first visit only for Mindbody's own date, an inferred one "earliest visit seen", the prospect and inactive clauses, the waiver's three states, "Not synced yet"; the package's name and terms, the renewal's count and dates in and out of this year, an estimate, on hand never "left", a staged lock with Mindbody's reading, month-to-month, the renewal's sentence only when it adds something, the source line once, the conversation only with no renewal on the books; `tabTone` never alert; the timeline oldest first with the era dashed first, paid-in-full pills and sessions, UTC months, no era for a whole story, an undated one otherwise, none for a prior record of no sessions; `accountTabHint` in every case and short; `accountGlance` full and sparse; no lookbehind), `MembershipSection.render.test.tsx` (18, moved from `ContractPanel.render.test.tsx` with its four cases — the tier, the lock through the form with the Auth uid, a saved lock and Use Mindbody's, the realm — plus the renewal read as it is, an estimate, the sessions banked, plum for lapsed and ended, the conversation in brand blue and a stylesheet with no hero or alert, the timeline, nothing on file, a studio staged with its own Save-bar home, On file with the new flags, How she found us through Edit and Done, no Mindbody ID in the fine print and the Migration Hub, a reader who may not edit, the section's head and anchors, no Tailwind), `AccountPage.render.test.tsx` (10: a linked client's facts, the ID once, no Sync button and no identity box; a nickname staged and Done only closing; an unlinked client's Edit, a name the rules would refuse, On file said once; a reader who may not edit; the notes verbatim, none, not synced, and no card for a typed-in client; every anchor once; the pronoun lede for she and he, the neighbours, Next "Done"; the Migration Hub; no Tailwind), `ClientCodex.render.test.tsx` (a cross-train reader's Account offers no editor and no Migration Hub but keeps the fine print; the home trainer's has them; `account-train-at` and `account-on-file` are anchors in the page once), `record-form.test.ts` (the cross-train studios' home), `page-meta.test.ts` (the Account line through `accountTabHint`, "7 on hand" beside an estimate), `scale.test.ts` (the new files on the scale; the budget at 80). Suite **299 files / 4,703 tests** (297 / 4,643 after phase 15; 4,695 before the review fixes below). Typecheck **4** (the phase-8 baseline; the same four errors). Build passes (`ClientProfileView` 628 kB, up from 623). Case check clean. The page was also rendered to static HTML with the real stylesheets and looked at in headless Edge at 744 (one column), 820 and 1024 (two columns) and 1366, light and dark, for a linked client, a typed-in one and a cross-train reader: nothing cut off.

**Review fixes, before the commit.** (1) A package paid in full or on banked sessions had lost its run-out date: the card dropped the renewal's line when on track, and the date column fell through to "No end date on file". It now says "Runs out around … (estimated)". (2) A contract whose end has passed says "Contract ended", not "ends"; a used-up package with no contract is dated "Ended …" by the job's own end. (3) An unknown renewal printed its first data gap twice (it is the status sentence too); now once. (4) On hand is counted ONE way, `onHandTotal` (every pricing option with sessions, as the fine print lists them): the sub-toggle had read the snapshot's `sessionsOnHand`, which leaves out an unmatched pack, and said "5 on hand" beside the page's "8". Beside an estimate of what is left, the card now also says "8 on hand in Mindbody now". (5) The lede (`accountLede`) is true for a typed-in client ("as typed into Journey … until she is linked") and for a reader who may not edit ("Read only here"). (6) With the studios not loaded, Where she can train says "Studio names not loaded yet" instead of "No other studios to approve" over a raw id. (7) `accountGlance` for a client Mindbody does not hold says "Typed in Journey · not linked to Mindbody" with no waiver line, as the page does. Tests: `account.test.ts` +6 (paid in full and banked run-out, ended contract and used-up package, the gap once, one on-hand figure with an unmatched pack, the unlinked glance, the three ledes), `MembershipSection.render.test.tsx` +2 (the run-out date and the gap once; studios not loaded), `AccountPage.render.test.tsx` (the typed-in and read-only ledes), `page-meta.test.ts` ("8 on hand" counted off the pricing options).

**On an iPad (AJ):** signed in as a Life Transformer, at 744 and 834 portrait and in landscape, both themes — open a long-standing client and go to Account (the last segment; its line should say "95 sessions left", "7 on hand" or "package ended"). The ID card reads as facts with the Mindbody ID once and no Sync button; Set a nickname, type one, Done: the bar says "1 unsaved change · Account · Contact" and the header changes only after Save. The package shows the package's name, what is left and when it renews, with the FileMaker years as the first dashed tile; Lock the tier → 18 mo · paid in full → the headline changes and "Mindbody reads as" appears → Discard. Tap a studio under Where she can train: "Account · Where they can train" on the bar, and Show lands on the card. Open the fine print; the Migration Hub switches to Journey. As a cross-train trainer: every fact, no button that edits, no Migration Hub. For a client typed in during a Mindbody outage (a temporary profile): Edit on the ID card opens the name and contact boxes, and On file says it is not linked.

## Phase 17 — the Mindbody intake-notes matcher (AJ's decision 4)

**Account's Mindbody account notes card is the intake matcher now.** A studio types a new client's sign-up answers into Mindbody's account notes ("OCC: Retired dental hygienist. / MED: R TKA Mar 2024. BP managed w/ meds. / ACTIVITY: Pickleball 2x/wk, gardening. / GOALS: Keep up w/ grandkids. Camino!"), and the real client AJ looked at had all four in Mindbody and none in Journey. The card (`src/features/client-admin/IntakeNotesCard.tsx`, in phase 16's place and under its anchor, `account-mindbody-notes`) shows the notes one line at a time, verbatim — the label as the studio typed it, never re-cased — and for a line it recognises, a dashed chip for where it belongs ("→ Occupation", "→ Body", "→ Recreation", "→ Her why", as the mockup has it), a sentence about what Journey already has there, the one tap on offer and a door ("Open Occupation", "Open Watch-outs", "Open her why"). `intake.ts` is the pure half (`parseIntakeNotes`, `matchIntake`, `nextFieldValue`, `intakeGoalsLines`). Read `src/features/client-admin/README.md` → The intake matcher.

| Line | Goes to | Offered when | Saves |
| --- | --- | --- | --- |
| Occ | her job title (`occupation`) | the job title is EMPTY, and the line could be one (one line, at most 80 characters — the Work editor's box) | staged; the Save bar ("FORD · Occupation") |
| Activity | a pinned FORD Recreation detail, the words verbatim, `origin: "mindbody_intake"` | FORD answered, and no detail (live or archived, in any pillar) holds these exact words | at once, like every FORD detail — `fordStudioIdOf(client)`, the Auth uid |
| Goals | her why (`globalNotes`) | her why is EMPTY | staged; the Save bar ("Goals & Focus · The why") |
| Med | her medical history (`medicalHistory`), added after what is there | always, unless these exact words are already in it | staged; the Save bar ("Body & Pulse · Watch-outs") |

- **It never judges whether her medical notes are covered.** With anything in Body the row says "Body has 2 clinical flags and a medical history on file. Read both side by side; the app can't tell whether they say the same thing." and still offers "Add to medical history". Unit and render tests forbid "covered" and "match" in any medical sentence, and in the whole card.
- **Unknown is not nothing.** With FORD loading, failed, refused (a cross-train reader) or a client with no home studio, the Activity row says which and offers nothing — a second copy is the one harm a tap can do. The record's own fields are still offered then.
- **One tap, one detail.** The FORD tap is guarded by a ref as well as the disabled button (every tap on the card waits while one adds); the row says "Added to Recreation." at once, and a refused add keeps the offer with "Not added. Check your connection and try again." A staged line says "Now her job title. Nothing is saved until you tap Save changes on the bar at the bottom." with the Unsaved chip; Discard brings the offer back.
- **The parser splits only where a label says to.** Any short label at the start of a line (after a list mark too — "- Occ:"); in the middle of a line only a KNOWN label after a sentence break ("Occ: nurse. Med: asthma"); "Hobbies/Activities" is Activity, "Occupation & hobbies" (two kinds) is not known. "call re: payment", "7:00", "https://" and "Tom - husband" are never cut; a run of words before a colon is a sentence, not a label. A line with no label goes on with the one above it unless a blank line came between; HTML and entities are read as plain text. No lookbehind.
- **A cut line is never copied.** Notes as long as the sync keeps (1,000 characters) may have been cut: the card says Mindbody may hold more, and their last line is shown but not offered.
- **Her why shows the Goals lines, read only** ("At sign-up (Mindbody notes)" on Goals & Focus — the adapter now passes `signUpGoals`). Account is the one place a line is copied; FORD's bands have no intake slot, so a line cannot be added twice from two pages.
- **The FORD page says where the words came from.** A detail with `origin: "mindbody_intake"` reads "Pickleball 2x/wk, gardening. · from the Mindbody account notes, added by AJ" (`provenanceOf`, `page-model.ts`; a moment's meta says the same in place of the bare name), so her sign-up words never read as something a trainer heard. `FordOrigin` gains the value; the rules do not read `origin`, so there is no rules change.

**No Firestore structure, rules, index, Mindbody or Functions change.** The only data-shape change is the new `FordOrigin` value (a string the rules never check). The FORD write is `createFordEntry` as every FORD writer calls it; the field taps are the record form's one `updateDoc` of the changed fields. No rules test changed, so this phase needs no `test:rules` run.

**Overnight defaults used** (reversible; each in one place): Med lines always offer "Add to medical history" (append) unless that exact line is already present, never judging coverage (`intake.ts` → `medicalMatch`); FORD details from intake are labelled `origin: "mindbody_intake"` (`IntakeNotesCard.tsx`, `ford/types.ts`); pronouns from the gender Mindbody holds ("Her why", "Use as her why", "Now her job title"). **Held, as asked:** an Activity add refreshes `fordSummary` like every FORD save, so while `fordSummary.pinned` is still written the line is copied there too (up to three per pillar), exactly as today.

**Deviations from the plan, and why.**
- **An Occ line that could not be a job title is not offered.** INTEGRATION sends Occ to `occupation` (the job title) when it is empty. A multi-line line, or one longer than the Work editor's 80-character box (`JOB_TITLE_MAX`, moved from `LifeBaseline.tsx` into `life.ts` so the pure matcher can read it), would land in a one-line box that cannot hold it; the row says so instead, with the door to Occupation.
- **`workSentence` keeps a job title that is itself a retirement** (`client-life/life.ts`). The Occ tap copies "Retired dental hygienist." verbatim, and the Work band then read "Work not recorded yet" — every title starting "Retired" was dropped as a status. Now such a title IS the band's sentence ("Retired dental hygienist.", "Retired teacher — was on their feet"); only the old list's two status entries ("Retired (Active Lifestyle)", "Retired (Sedentary Lifestyle)") still say nothing about the work. The FORD band, the Overview's FORD lead and Body & Pulse's "her work" read it.
- **Activity is offered beside what Journey already has** (the goals-story spec offered it only on an empty Recreation). FORD is a list, not a field: adding a detail replaces nothing, and whether "Moderate · Golf" says what "Pickleball 2x/wk" says is the same judgement decision 4 forbids for the medical line. So it is offered unless these exact words are already a detail — in any pillar, or archived (then the row says it was archived, and does not offer it again over the team's decision).
- **The medical row lists what Body holds as "contraindications & constraints"** (Body's own label), not "contraindications".
- **Occ with a job title on file reads "Journey has: “Hygienist” as her job title."** rather than `workSentence`'s "(Hygienist)" (the spec's wording): with no work category the band sentence is only the title in brackets.
- **Unknown labels are labels only when short** (three words at most), so "Wife is Karen and she said: hello" stays one line of text; a leading list mark and joined labels ("Hobbies/Activities") are read, and none of these were in the spec.
- **`origin` reads "Added from the Mindbody account notes" in the To-file tray's words** (`ORIGIN_WORDS`), which the tray prints as "… by AJ, Mar 15"; an intake detail is filed, so the tray should never show one.
- **No `access.ts` in client-admin, and no `canEditClient`** (INTEGRATION): every tap needs the shell's `canEdit`; a reader who may not edit sees every line, sentence and door.
- **Tooling:** the file tool decoded the parser's U+00A0, U+2013, U+2014 and U+2022 regex escapes into the raw characters when the file was written (the no-break space invisible). They were put back with a Node script; KNOWN-TRAPS → tooling now says how.

**Tests.** `intake.test.ts` (new): the mockup's block as four lines with labels as written; case, full stop, space and dash forms, en and em dashes, joined labels and list marks; "Tom - husband", "call re: payment", "Med: knee. BP: 120/80", "7:00", "https://" and a sentence before a colon all whole; carry-on lines and the blank-line break; HTML and entities; CRLF and CR; empty and null; spaces collapsed; exactly 1,000 characters truncated and 999 not; the Goals lines; no lookbehind in the source. The matcher: Occ on an empty record, never over a title, present once staged, what else Journey holds and FORD's count only once FORD answered, never a title the box could not hold; Activity offered on empty and beside other details, not when the words are a detail anywhere or archived, and unknown (with the sentence for each) while FORD is loading, failed, refused or has no studio; Goals only while empty, present, and in "his"; Med set on an empty Body, append with the read-both sentence, present only for the exact words, and never "covered"/"match" over six records; other lines offered nothing; a cut last line never offered; keys stable across re-reads; `nextFieldValue`; `intakeNorm`. `life.test.ts` (a retirement title kept). `page-model.test.ts` (`provenanceOf`, `ORIGIN_WORDS`). `AccountPage.render.test.tsx` (the notes line by line; the four offers and nothing for an unknown label; ONE `createFordEntry` on a double tap, with the home studio, the Auth uid and `mindbody_intake`, the card busy meanwhile, "Added to Recreation."; a refused add keeps the offer; the medical line appended and staged, the bar naming Watch-outs, Discard bringing the offer back; the job title and why staged with their own bar places, and never over what is there; nothing offered once the exact words are there; no FORD offer while FORD loads or failed; a reader who may not edit gets sentences and doors only; never "covered"). `FordPage.render.test.tsx` (the provenance beside a fact and in a moment's meta; a heard fact is its words alone). `ClientCodex.render.test.tsx` (through the tab's one load: the FORD add stamped `s1` / `uid-ann` / `mindbody_intake` on the ONE FORD listener, the medical line staged and never written; the Goals line under her why on Goals & Focus; a cross-train reader offered nothing, with no FORD listener). `scale.test.ts` gains `IntakeNotesCard.tsx` and `intake.ts`. Suite **300 files / 4,758 tests** after the review fixes below (299 / 4,703 after phase 16). Typecheck **4** (the phase-8 baseline; the same four errors, diffed). Build passes (`ClientProfileView` 641.7 kB, up from 628 kB — Vite's 500 kB note is advisory). Case check clean. The card was also rendered to static HTML with the real stylesheets and looked at in headless Edge at 744 (one column), 1024 and 1366 (beside the ID card), light and dark: nothing cut off, the chip beside the words, the taps and doors wrapping under the sentence.

**Review fixes, before the commit.** (1) "These exact words are already there" — the medical history, and her why — compares WHOLE words in order, punctuation aside (`holdsWords`). It was a substring test, so "Med: knee" read as already in a history of "Kneecap fracture 2010" ("Med: hip" in "HIPAA form signed"): a false medical claim that hid the one offer the medical line always gets. (2) A known label straight after an empty one ("Occ:   Med: asthma", a blank field of the form with its line break lost) starts its own row; it had been read as one Occ line, and "Med: asthma" was offered as her job title. (3) A line whose label the matcher does not know is shown exactly as typed, colon and all — "Please call re: billing" had been cut into the label "Please call re" and the words "billing". It still starts its own row, and a known label after a break in it still splits it. (4) **The FORD tap follows the FORD create rule.** `codexAccess` gains `fordWritable` (trains at or leads her home studio, the grant and Demo Mode included — `worksAt`, `leadsStudio`, `hasRunOfDemo`, as `isTrainerOfStudio` reads). An administrator or franchise owner who works elsewhere reads FORD, and an administrator may edit the record, but the create rule has no clause for either: the tap was offered and then refused with "Check your connection". The row now says "Only a trainer at her home studio can add a FORD detail, so it isn't offered here.", and the record's own fields are still offered. (5) On Goals & Focus a Goals line the sync may have cut short ends in "…", so it never reads as her whole sentence. (6) The README says the notes are written by the Mindbody webhook and Master Sync. (7) A moment's meta line that leads with the provenance starts with a capital ("From the Mindbody account notes, added by AJ · Jan 1"). Tests: `intake.test.ts` (whole words — knee/Kneecap, hip/HIPAA, BP/BPPV, strength/strengthening, punctuation aside; the empty-label split, three deep; unknown labels kept whole and still starting a row; the ellipsis; the create-rule gate), `access.test.ts` (`fordWritable` for every role, a client with no studio and nobody signed in), `AccountPage.render.test.tsx` (+1: an administrator elsewhere gets the sentence and no FORD tap; the unknown line found by its words as typed), `ClientCodex.render.test.tsx` (+1: a Founder at another studio, through the tab's one load), `FordPage.render.test.tsx` (the capital). Typecheck 4, build and case check clean. **Left as it was:** the FORD page's own Add a detail (`fordCanAdd`, phase 10) has the same gap for an administrator who works elsewhere; `fordWritable` is there for the cleanup phase to close it.

**On an iPad (AJ):** signed in as a Life Transformer at the client's home studio (not as a Founder — see the review fixes: an administrator who works elsewhere is not offered the FORD tap), at 744 and 834 portrait and in landscape, both themes — open a client whose Mindbody notes have "Occ:", "Med:", "Activity:" and "Goals:" lines and go to Account. Each line is its own row with its label as the studio typed it and a dashed "→ Occupation" chip. Tap **Add to Recreation** twice quickly: ONE detail, and FORD → Recreation shows it with "from the Mindbody account notes, added by {you}". Tap **Add to Occupation** and **Add as medical history**: the rows say "Nothing is saved until you tap Save changes…", the bar says "2 unsaved changes · FORD · Occupation, Body & Pulse · Watch-outs"; Save, then open FORD → Occupation (the Work band reads the line as written) and Body & Pulse → Watch-outs (the history ends with the line). Open Goals & Focus: "At sign-up (Mindbody notes)" under her why. As a cross-train trainer: every line and sentence, the doors, no tap, and "FORD is kept by her home studio".
