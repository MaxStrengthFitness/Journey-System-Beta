import type { CapturePreset } from "./capture";

/** Placeholder until the Capture phase lands. */
export function CaptureSheet(_: { open: boolean; preset: CapturePreset | null; onOpenChange: (o: boolean) => void }) {
  void _;
  return null;
}
