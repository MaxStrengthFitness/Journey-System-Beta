/**
 * RETIRED — Machine authoring, Sep 2026.
 *
 * The catalog's list and its editor-in-a-dialog were split in two and
 * rebuilt:
 *
 *   the list    -> CatalogList.tsx        (adm rows, ordered, with gaps named)
 *   the editor  -> CatalogMachineEditor.tsx -> editor/MachineEditor.tsx
 *   the defaults -> definition-defaults.ts
 *
 * Kept as an empty file only because this working copy is on a mount where
 * Claude cannot delete. Safe to remove: nothing imports it.
 */
export {};
